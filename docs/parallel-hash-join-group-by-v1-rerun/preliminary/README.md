# Interrupted preliminary run

This no-op-breaker run was stopped when the benchmark context was found not to
exercise active owner/worker circuit-breaker overhead. The original environment,
commands and incomplete primary samples retain their content, with whitespace
normalized for browsing. The parent raw-output.tar.gz keeps the byte-exact output. They do not enter
the final summary or primary acceptance gate. The final matrix restarts every case
with active breakers and the server-default throttle; see the parent report.
