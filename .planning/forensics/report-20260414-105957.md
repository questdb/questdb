# Forensic Report

**Generated:** 2026-04-14 10:59 UTC
**Scope:** ROADMAP.md consistency after phase 5 / phase 11 back-fill edits
**Problem:** User requested re-check of the roadmap after the latest fixes

---

## Evidence Summary

### Working Tree
- `git status` shows `M .planning/ROADMAP.md` — uncommitted edits from this session's back-fill.
- **`.planning/` is gitignored** (`.gitignore:56`) but many files were committed before the ignore landed; git tracking is therefore uneven (see Anomaly 4).

### ROADMAP.md structure audit
Read top-to-bottom (194 lines):

| Region | Lines | Finding |
|--------|-------|---------|
| Overview paragraph | 5 | Describes phase 2 as "in progress" — stale by 11 phases |
| Phase Numbering note | 9–13 | Documents decimal phases (`2.1`, `2.2`) that never exist in this project |
| Top-of-file checklist | 15–20 | Lists only phases 1–6; phase 4 unchecked; phase 5 unchecked and stale; **phases 7–11 completely absent** |
| Phase Details | 24–86 | Phases 1–5 detailed |
| **Progress table** | 88–105 | Inserted between phase 5 and phase 6 details, **splitting Phase Details into two groups** |
| Phase Details (cont.) | 107–193 | Phases 6–11 detailed |
| Phase 5 details | 75–86 | Plan still listed `- [ ]` (unchecked) even though progress table marks it "Absorbed" |
| Plans-line format | scattered | 6 phases use `**Plans:** 1 plan`; 3 use `**Plans:** 1/1 plans complete`; phase 11 uses a third variant |

### STATE.md staleness
| Field | Current value | Reality |
|-------|---------------|---------|
| `status` | `verifying` | work closed, should be `complete` or `paused` |
| `stopped_at` | `Completed 10-01-PLAN.md` | phase 10 was not the last; phase 11 landed afterwards |
| `last_updated` | `2026-04-13T14:41:44` | stale; today is 2026-04-14 |
| `progress.total_phases` | 10 | should be 11 |
| `progress.completed_phases` | 8 | 11 phases complete / absorbed |
| `progress.total_plans` | 9 | should be 10 |
| `progress.completed_plans` | 8 | should be 10 |
| `progress.percent` | 89 | should be 100 |
| `**Current focus:**` line 24 | "Phase 3 - Keyed Fill Cursor" | eight phases stale |
| Current Position section | "Phase: 3 of 5" + `[######....] 60%` | eight phases stale |
| Blockers/Concerns lines 102–103 | phase 2 concerns | both resolved by phases 5–10 |

### Uncommitted artifacts (untracked, gitignored)
- `.planning/phases/05-verification-and-hardening/05-01-SUMMARY.md` (new)
- `.planning/phases/11-hardening-review-findings-fixes-and-missing-test-coverage/11-01-PLAN.md` (new)
- `.planning/phases/11-hardening-review-findings-fixes-and-missing-test-coverage/11-01-SUMMARY.md` (new)
- `.planning/forensics/report-20260414-105203.md` (previous forensics report)

### gsd-tools sanity
- `progress bar --raw` → `[████████████████████] 10/10 plans (100%)`
- `init progress` → `completed_count: 10`, `current_phase: null`, `has_work_in_progress: false`

The tooling view is now consistent with the intended state. The **documents themselves** are internally inconsistent — that is the remaining problem.

---

## Anomalies Detected

### 1. Roadmap Progress table placed mid-document (Confidence: HIGH)
**Evidence:** ROADMAP.md line 88 `## Progress` heading sits between phase 5 detail block (ends line 86) and phase 6 detail block (starts line 107). Readers encounter the progress table halfway through the phase narrative.

**Interpretation:** Phase 6 was inserted after the initial roadmap was written, and subsequent phases 7–11 continued appending after the progress table rather than before it. The file grew by addition without structural maintenance.

### 2. Top-of-file Phase checklist severely stale (Confidence: HIGH)
**Evidence:** Lines 15–20 list six phases (1–6). Phase 4 is unchecked despite progress table showing "Complete 2026-04-10". Phase 5 is unchecked and labeled "in progress" despite progress table showing "Absorbed". Phases 7–11 are not listed at all.

**Interpretation:** The checklist was written early and never updated when new phases were added via `/gsd-add-phase`. It contradicts the progress table directly below it.

### 3. Phase 5 detail block contradicts its own progress entry (Confidence: HIGH)
**Evidence:** Progress table row 99: `| 5. Verification and Hardening | 1/1 | Absorbed by 7–10 | 2026-04-10 |`. Detail block line 84–86: `**Plans:** 1 plan` + `- [ ] 05-01-PLAN.md -- Fix generateFill() metadata timestamp index...`. The unchecked checkbox and "1 plan" string read as incomplete, directly contradicting the "1/1" in the progress table.

**Interpretation:** I updated the progress row in the back-fill session but did not propagate the status to the detail block. Leftover drift from the current session.

### 4. Uneven git tracking of `.planning/` (Confidence: MEDIUM)
**Evidence:** `.gitignore:56` ignores `.planning/`, but `git ls-files .planning/` returns 15+ tracked files spanning phases 02, 04, 05, 06 (SUMMARY only), 07, 08 (SUMMARY only), 09, 10 (PLAN only). Phase 3 and phase 11 have zero tracked files. The new back-fill files for phase 5 / phase 11 are untracked by virtue of the ignore rule.

**Interpretation:** At some point `.planning/` was added to `.gitignore` but prior commits that tracked individual files remain on branch. New files land untracked. This produces a paper trail where some phases are half-in-git and some are local-only, inconsistently.

### 5. STATE.md eight phases stale (Confidence: HIGH)
**Evidence:** STATE.md `Current focus: Phase 3 - Keyed Fill Cursor`, `Phase: 3 of 5`, `Progress: [######....] 60%`. Progress frontmatter block lists 10 total phases with 89% completion. Both views contradict the progress table in ROADMAP.md (11/11) and gsd-tools (10/10).

**Interpretation:** STATE.md's manual narrative section was written once at phase 3 completion and never updated. The frontmatter was updated automatically up through phase 10, then stopped when phase 11 landed without a SUMMARY.

### 6. Overview paragraph outdated (Confidence: MEDIUM)
**Evidence:** Line 5: "Work progresses from optimizer gating (done) through non-keyed fill (in progress), keyed fill with cartesian product semantics, cross-column prev (new feature), and full verification against the existing 302-test suite."

**Interpretation:** Overview describes a phase 2 in-progress state. The document has since grown through phases 7 (type-safe PREV), 8 (regression fixes), 9 (critical review), 10 (offset), 11 (hardening) — none mentioned.

### 7. Decimal phases explainer for phases that don't exist (Confidence: LOW)
**Evidence:** Lines 9–13 describe the decimal-phase (`2.1`, `2.2`) numbering scheme with "INSERTED" markers. No decimal phase appears anywhere in this roadmap.

**Interpretation:** Boilerplate from the roadmap template that was never deleted. Harmless but adds noise for readers.

### 8. Inconsistent "**Plans:**" line format (Confidence: LOW)
**Evidence:**
- Phases 2, 3, 4, 5, 7, 8, 9: `**Plans:** 1 plan`
- Phases 6, 10: `**Plans:** 1/1 plans complete`
- Phase 11: `**Plans:** 1/1 plans complete (retroactive — work landed before plan was written)`

**Interpretation:** Three writers / three conventions. Not a bug, but signals to a reader that the document evolved without style discipline.

---

## Root Cause Hypothesis

The roadmap is **structurally consistent at the tooling layer** (gsd-tools sees 10/10 complete) but **internally inconsistent at the document layer**: the progress table contradicts the top-of-file checklist, the phase 5 detail block contradicts its own progress row, STATE.md contradicts both, and the Overview still describes phase 2 as in-flight.

Two forces produced the drift:
1. **Phases added by `/gsd-add-phase` don't update the Overview, top-of-file checklist, or STATE.md narrative section** — only the progress table + phase details get new rows.
2. **Status changes (like "Absorbed" for phase 5) are edited in one place** (the progress table) but the corresponding detail block still shows an unchecked checkbox.

Neither force is data loss; both are document-maintenance gaps that compound as phases accumulate.

---

## Recommended Actions

1. **Restructure ROADMAP.md** — move `## Progress` to the bottom of the file (after all phase details), so Phase Details is one contiguous section. Low-risk reorganization.
2. **Rewrite the top-of-file checklist (lines 15–20)** — include phases 7–11, mark phases 4/5 per current progress, or remove the checklist entirely since the progress table already conveys this information.
3. **Sync phase 5 detail block** — change `- [ ] 05-01-PLAN.md ...` to `- [x]` and update `**Plans:** 1 plan` to `**Plans:** 1/1 plans complete (absorbed — see SUMMARY)`.
4. **Update STATE.md** — fix `status`, `stopped_at`, progress frontmatter, `Current focus`, Current Position section, and retire the two stale Blockers/Concerns rows.
5. **Refresh the Overview paragraph** — replace the stale "in progress" framing with a one-sentence "done" description.
6. **Delete the decimal-phases explainer** (lines 9–13) since no decimal phases exist in this project.
7. **Normalize "**Plans:**" line format** across phases (pick `1/1 plans complete` or the terser `1 plan`).
8. **Decide `.planning/` git policy** — either remove `.planning/` from `.gitignore` and commit everything, or `git rm --cached` the currently-tracked planning files so the ignore rule takes effect uniformly. The current half-and-half state is confusing.

Actions 1–5 are in-session cleanup; 6–7 are editorial; 8 is a policy call that likely wants user input.

---

*Report generated by `/gsd-forensics`. Paths relative to repo root.*
