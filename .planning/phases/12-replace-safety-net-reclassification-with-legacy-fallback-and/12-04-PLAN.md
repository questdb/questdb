---
phase: 12
plan: 04
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
files_modified:
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByNanoTimestampTest.java
  - core/src/test/java/io/questdb/test/griffin/ExplainPlanTest.java
autonomous: true
requirements:
  - COR-01
  - COR-02
  - COR-04
  - PTSF-04
  - CONTEXT-item-5
  - CONTEXT-item-7
  - CONTEXT-item-8
  - CONTEXT-item-9
  - CONTEXT-item-10
  - CONTEXT-item-11
  - CONTEXT-item-12
  - CONTEXT-item-13
  - D-10
user_setup: []

must_haves:
  truths:
    - "Five retro-fallback regression tests exist in SampleByFillTest and assert the legacy plan is picked"
    - "11 grammar regression tests (3 positive + 8 negative) exist in SampleByFillTest covering D-05 through D-09"
    - "Five FILL_KEY tests exist in SampleByFillTest for UUID, Long256, Decimal128, Decimal256, geo-no-prev-yet"
    - "testFillToNullTimestamp exists and asserts TO null::timestamp produces bounded output"
    - "testSampleByFillNeedFix in SampleByTest is restored to master's 3-row expected output; the 'two rows per bucket' comment is deleted"
    - "Approximately 15 assertSql call sites in SampleByFillTest converted to assertQueryNoLeakCheck per the D-10 rule"
    - "Plan-text assertions across SampleByFillTest, SampleByTest, SampleByNanoTimestampTest, ExplainPlanTest are refreshed to include the unconditional fill=null|prev|value attribute"
    - "Full suite (SampleByFillTest + SampleByTest + SampleByNanoTimestampTest + ExplainPlanTest) exits 0"
  artifacts:
    - path: "core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java"
      provides: "19 new tests + ~15 assertSql→assertQueryNoLeakCheck conversions + plan-text refresh"
      contains: "testFillPrevExpressionArgDecimal128Fallback"
    - path: "core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java"
      provides: "testSampleByFillNeedFix restored to 3-row master expectation + plan-text refresh"
      contains: "testSampleByFillNeedFix"
  key_links:
    - from: "Retro-fallback tests"
      to: "legacy Sample By factory"
      via: "assertPlanNoLeakCheck on 'Sample By' (not 'Sample By Fill')"
      pattern: "Sample By\\b"
    - from: "Grammar tests"
      to: "SqlException at specific positions"
      via: "assertExceptionNoLeakCheck with position + message"
      pattern: "assertExceptionNoLeakCheck"
    - from: "FILL_KEY tests"
      to: "FillRecord.getLong128Hi/Lo, getDecimal128/256, getLong256"
      via: "keyed SAMPLE BY FILL(NULL) with 128/256-bit key column, assert key values in fill rows"
      pattern: "testFillKeyed(Uuid|Long256|Decimal128|Decimal256)"
---

<objective>
Land the phase 12 test coverage: 19 new tests (5 retro-fallback + 8 grammar +
5 FILL_KEY + 1 TO-null), the `testSampleByFillNeedFix` restoration to master's
3-row output, ~15 `assertSql`→`assertQueryNoLeakCheck` conversions per the
D-10 rule, and a refresh of every `assertPlanNoLeakCheck` call site across
four test files so the new unconditional `fill=null|prev|value` attribute
from plan 02 lines up with expected plan text.

Purpose: Without these tests, the phase 11 production fixes (FILL_KEY
dispatch for 128/256-bit types, geo null sentinels) and the phase 12
production fixes (retro-fallback, grammar rules, hasExplicitTo guard) can
silently regress — no existing test covers them.
Output: Fully green `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test`.
Depends on plans 02 and 03 (production code must be in place before tests can
pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-CONTEXT.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-RESEARCH.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-VALIDATION.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-02-SUMMARY.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-03-SUMMARY.md

<interfaces>
<!-- Test patterns to mirror. Extracted from the current codebase. -->

From core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — standard test shape:
```java
@Test
public void testFillPrevKeyedCte() throws Exception {
    assertMemoryLeak(() -> {
        execute("CREATE TABLE weather (ts TIMESTAMP, city SYMBOL, temp DOUBLE) TIMESTAMP(ts) PARTITION BY DAY");
        execute("INSERT INTO weather VALUES " +
                "('2024-01-01T00:00:00Z', 'London', 10.0), " +
                "('2024-01-01T02:00:00Z', 'London', 12.0)");
        assertQueryNoLeakCheck(
                "ts\tcity\ttemp\n" +
                "2024-01-01T00:00:00.000000Z\tLondon\t10.0\n" +
                "2024-01-01T01:00:00.000000Z\tLondon\t10.0\n" +
                "2024-01-01T02:00:00.000000Z\tLondon\t12.0\n",
                "WITH w AS (SELECT ts, city, temp FROM weather) " +
                "SELECT ts, city, avg(temp) temp FROM w SAMPLE BY 1h FILL(PREV)",
                "ts", false, false);
    });
}
```

assertQueryNoLeakCheck signature (from AbstractGriffinTest):
```java
protected void assertQueryNoLeakCheck(
    String expected,
    String query,
    String expectedTimestamp,
    boolean supportsRandomAccess,   // MUST be false for fast-path fill
    boolean expectSize              // MUST be false (size() = -1) for fast-path fill
) throws Exception
```

assertPlanNoLeakCheck signature:
```java
protected void assertPlanNoLeakCheck(
    CharSequence query,
    CharSequence plan
) throws Exception
```

assertExceptionNoLeakCheck signature:
```java
protected void assertExceptionNoLeakCheck(
    CharSequence query,
    int position,           // column offset of the error
    CharSequence message    // substring of the exception message
) throws Exception
```

From SampleByTest.java (around line 5943) — `testSampleByFillNeedFix` currently asserts 6 rows per PR #6946. Target (master's expectation): 3 rows. Exact current body must be read in place.

Target plan text for the new `fill=` attribute:
- `FILL(NULL)`: `Sample By Fill\n  fill=null\n  stride='...'\n  ...`
- `FILL(PREV)`: `Sample By Fill\n  fill=prev\n  stride='...'\n  ...`
- `FILL(42)`: `Sample By Fill\n  fill=value\n  stride='...'\n  ...`
(Plan 02 task 1 added the unconditional emission — exact ordering in `toPlan` should be verified during task 3 of this plan.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 5 retro-fallback tests + 1 TO null test + testSampleByFillNeedFix restoration</name>
  <files>
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java,
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
  </files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — read the entire file, especially existing `testFillPrev*` tests (lines ~800-1200) for data-setup patterns and assertPlanNoLeakCheck usage
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java lines 5900-6000 — `testSampleByFillNeedFix` current body
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java line 807 (or wherever `testFillPrevCrossColumnUnsupportedFallback` sits — CONTEXT mentions it; confirm via grep)
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-CONTEXT.md "Retro-fallback regression tests" and item 4 (TO null) and item 5 (testSampleByFillNeedFix)
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-RESEARCH.md "Phase Requirements → Test Map"
    - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillNullNotKeyedRecordCursorFactory.java and SampleByFillPrevNotKeyedRecordCursorFactory.java — for the legacy plan-text expectations (`Sample By` type, column list)
  </read_first>
  <action>
    **Sub-task A — Add 5 retro-fallback tests** to `SampleByFillTest.java`. Place them in alphabetical order among other `testFillPrev*Fallback*` tests (near the existing `testFillPrevCrossColumnUnsupportedFallback` if present). Each test:

    1. Sets up a table with an aggregate type that is unsupported on the fast path.
    2. Asserts the plan shows the legacy factory (contains `Sample By`, NOT `Sample By Fill`).
    3. Asserts the data output matches master's legacy-cursor expectations.
    4. Uses `assertMemoryLeak` + `assertPlanNoLeakCheck` + `assertSql` (or `assertQueryNoLeakCheck` if the random-access / size properties match).

    Test list (names verbatim from CONTEXT.md success criterion #11):

    - `testFillPrevExpressionArgDecimal128Fallback` — `SELECT ts, sum(d128_col * 1::decimal(25,2)) FROM t SAMPLE BY 1h FILL(PREV)` where `d128_col` is `DECIMAL(25,2)` (DECIMAL128 output). The expression `d128_col * 1::decimal(25,2)` returns DECIMAL128, which is not fast-path-supported, so the codegen retro-fallback fires.
    - `testFillPrevExpressionArgStringFallback` — `SELECT ts, first(substr(s, 1, 5)) FROM t SAMPLE BY 1h FILL(PREV)` where `s` is STRING. The `substr` expression output is STRING.
    - `testFillPrevCaseOverDecimalFallback` — `SELECT ts, first(CASE WHEN v > 0 THEN d256_col ELSE NULL::decimal(39,2) END) FROM t SAMPLE BY 1h FILL(PREV)` where `d256_col` is `DECIMAL(39,2)` (DECIMAL256 output).
    - `testFillPrevLong128Fallback` — `SELECT ts, first(long128_col) FROM t SAMPLE BY 1h FILL(PREV)` where `long128_col` is `LONG128`. Note: the Tier 1 gate (plan 01 task 3) should already reject this at the gate, so this test asserts the gate-rejection path, not the retro-fallback path. Both paths produce the same legacy plan — the test doesn't care which fires.
    - `testFillPrevIntervalFallback` — `SELECT ts, first(interval_col) FROM t SAMPLE BY 1h FILL(PREV)` where `interval_col` is `INTERVAL`. Same as above — gate-rejected.

    For each, use a minimal data set (3 rows with a 1-row gap), and assert:
    - Plan text contains `Sample By` (legacy factory) via `assertPlanNoLeakCheck`.
    - Output rows match what the legacy cursor produces for the same query (run the query against master to capture, or derive from domain knowledge).

    If any of these types require specific CREATE TABLE syntax (e.g., `DECIMAL(25,2)`, `LONG128`, `INTERVAL`), use the existing QuestDB syntax — check other tests in SampleByFillTest that already use these types.

    **Sub-task B — Add `testFillToNullTimestamp`** (CONTEXT item 4 + success criterion #14).

    ```java
    @Test
    public void testFillToNullTimestamp() throws Exception {
        assertMemoryLeak(() -> {
            execute("CREATE TABLE t (ts TIMESTAMP, v DOUBLE) TIMESTAMP(ts) PARTITION BY DAY");
            execute("INSERT INTO t VALUES " +
                    "('2024-01-01T00:00:00Z', 1.0), " +
                    "('2024-01-01T02:00:00Z', 2.0)");
            // TO null::timestamp evaluates to LONG_NULL at runtime. Without the
            // hasExplicitTo guard (plan 02 task 1), the fill loop would emit
            // rows up to Long.MAX_VALUE. With the guard, emission is bounded
            // by the last data bucket.
            assertQueryNoLeakCheck(
                    "ts\tavg\n" +
                    "2024-01-01T00:00:00.000000Z\t1.0\n" +
                    "2024-01-01T01:00:00.000000Z\tnull\n" +
                    "2024-01-01T02:00:00.000000Z\t2.0\n",
                    "SELECT ts, avg(v) FROM t SAMPLE BY 1h FROM '2024-01-01' TO null::timestamp FILL(NULL)",
                    "ts", false, false);
        });
    }
    ```
    Place alphabetically among other `testFillTo*` tests.

    **Sub-task C — Restore `testSampleByFillNeedFix`** in `SampleByTest.java` (CONTEXT item 5).

    Current state (per CONTEXT.md): the test expects 6 rows for a single-symbol gap query. Master's expectation is 3 rows (one per bucket, no duplicates).

    Action:
    1. Read the test body at `SampleByTest.java` around line 5943.
    2. Replace the 6-row expected output with master's 3-row output. Read master's version via `git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java` (or comparable) — find the `testSampleByFillNeedFix` body and copy the exact expected-output string.
    3. Delete the comment "The fill cursor emits fill rows per unique key combination. Since 'open' (first()) has distinct prev values per bucket, the fill produces two rows per bucket for the gap hour." (or whatever the exact "two rows per bucket" rationale comment is).
    4. If the test was converted from `assertSql` to `assertQueryNoLeakCheck` on the PR, revert to whatever master uses.

    This restoration MUST happen only after plan 03's retro-fallback changes land — otherwise the test would fail because the safety-net reclassification would still produce 6 rows. Plan 03 is a `depends_on` of this plan.

    Do NOT add banner comments. Keep all new tests in strict alphabetical order.
  </action>
  <verify>
    <automated>mvn -pl core -Dtest='SampleByFillTest#testFillPrev*Fallback,SampleByFillTest#testFillToNullTimestamp,SampleByTest#testSampleByFillNeedFix' test -q 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "testFillPrevExpressionArgDecimal128Fallback\|testFillPrevExpressionArgStringFallback\|testFillPrevCaseOverDecimalFallback\|testFillPrevLong128Fallback\|testFillPrevIntervalFallback" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns 5 matching method declarations.
    - `grep -n "testFillToNullTimestamp" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns one method declaration.
    - Each of the 5 fallback tests contains `assertPlanNoLeakCheck` with an expected string that contains literal text `Sample By` (legacy) but NOT `Sample By Fill` (fast path).
    - `testSampleByFillNeedFix` in `SampleByTest.java` no longer contains the comment mentioning "two rows per bucket". Verify: `grep -A 2 "testSampleByFillNeedFix" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java | grep -c "two rows per bucket"` returns `0`.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevExpressionArgDecimal128Fallback' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevExpressionArgStringFallback' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevCaseOverDecimalFallback' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevLong128Fallback' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevIntervalFallback' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillToNullTimestamp' test` exits 0.
    - `mvn -pl core -Dtest='SampleByTest#testSampleByFillNeedFix' test` exits 0.
  </acceptance_criteria>
  <done>
    All 5 retro-fallback tests assert the legacy plan is picked and output matches master. `testFillToNullTimestamp` exercises the hasExplicitTo LONG_NULL guard. `testSampleByFillNeedFix` is back to master's 3-row expectation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the 8 grammar tests + 5 FILL_KEY tests</name>
  <files>core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java</files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — existing `testFillPrevReject*` or error tests (if any) for `assertExceptionNoLeakCheck` pattern; existing `testFillKeyed*` tests (e.g. `testFillPrevKeyedCte` around line 960) for keyed-query setup
    - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java lines 705-1140 (FillRecord getters — for the exact method names exercised by each FILL_KEY test)
    - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java — the exact error messages inserted in plan 03 task 1 (for the `assertExceptionNoLeakCheck` message assertion)
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-CONTEXT.md "Grammar block" section (items 6-7 and D-05 through D-09), CONTEXT items 8-12 (FILL_KEY tests)
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-RESEARCH.md "Phase Requirements → Test Map"
  </read_first>
  <action>
    **Sub-task A — 8 grammar tests** in `SampleByFillTest.java`. Each uses `assertExceptionNoLeakCheck(query, position, messageSubstring)` (or a positive `assertQueryNoLeakCheck` for the accept tests). Place in alphabetical order.

    Negative tests (6):
    - `testFillPrevRejectTimestamp` — query: `SELECT ts, avg(v) FROM t SAMPLE BY 1h FILL(PREV(ts))`. Expect SqlException with message containing "PREV cannot reference the designated timestamp column". Position = offset of `ts` inside `PREV(ts)` in the query string.
    - `testFillPrevRejectMutualChain` — query: `SELECT ts, avg(a) a, avg(b) b FROM t SAMPLE BY 1h FILL(PREV(b), PREV(a))` (where `a` and `b` are DOUBLE columns). Expect "FILL(PREV) chains are not supported: source column is itself a cross-column PREV".
    - `testFillPrevRejectThreeHopChain` — query: `SELECT ts, avg(a) a, avg(b) b, avg(c) c FROM t SAMPLE BY 1h FILL(PREV(b), PREV(c), PREV)` (a→b, b→c, c→self). `fillModes[a]=b (>=0)`, `fillModes[b]=c (>=0)`, `fillModes[c]=FILL_PREV_SELF (-2)`. For column `a`: `fillModes[a]=b (>=0)` AND `fillModes[b]=c (>=0)` — chain rejection fires.
    - `testFillPrevRejectFuncArg` — `FILL(PREV(concat(a, 'x')))` → SqlException "PREV argument must be a single column name".
    - `testFillPrevRejectMultiArg` — `FILL(PREV(a, b))` → same message.
    - `testFillPrevRejectNoArg` — `FILL(PREV())` → same message. (Grammar check: may already be caught by the parser before reaching generateFill; if so, confirm the test still produces the right error — it may be a different position.)
    - `testFillPrevRejectBindVar` — `FILL(PREV($1))` → "PREV argument must be a single column name". Use a bind-var-capable context for this test. If the query shape can't reach generateFill with a bind variable (parser may reject earlier), the test should still assert the error is surfaced with the correct message.
    - `testFillPrevRejectTypeMismatch` — `SELECT ts, avg(a::long) a, avg(b) b FROM t SAMPLE BY 1h FILL(PREV(a), PREV(b))` where `a` is LONG-output and `b` is DOUBLE-output. The expression `PREV(a)` targets the DOUBLE column `b`... wait — re-read the grammar. `FILL(PREV(src), ...)` — the source column is the argument. So if `a` (LONG) is used as the source for a DOUBLE target, the tags differ (INT-family vs DOUBLE). Construct the test so the source type-tag differs from the target type-tag. Exact query to be finalized during implementation based on what aggregate shapes QuestDB supports; the test must trigger D-09 specifically.

    That's 6+2 positive = 8. Wait — the count is negative 5 + positive 2 + malformed 4 + type-mismatch 1 = 12, but CONTEXT says 8. Re-read:

    Per CONTEXT.md "Tests for the grammar":
    - `testFillPrevRejectTimestamp` (1)
    - `testFillPrevSelfAlias` (1, positive)
    - `testFillPrevRejectMutualChain` (1)
    - `testFillPrevRejectThreeHopChain` (1)
    - `testFillPrevAcceptPrevToSelfPrev` (1, positive)
    - `testFillPrevAcceptPrevToConstant` (1, positive)
    - `testFillPrevRejectFuncArg`, `testFillPrevRejectMultiArg`, `testFillPrevRejectNoArg`, `testFillPrevRejectBindVar` (4 — collectively one concept: malformed PREV)
    - `testFillPrevRejectTypeMismatch` (1)

    That's 11 tests. CONTEXT success criterion #10 says "Eight grammar regression tests". The four malformed-PREV tests may be consolidated or the count is a rounding. Implement all 11 tests (comprehensive coverage), and note in the summary that we delivered 11 not 8. CONTEXT's specific list is the authority over the count.

    Positive tests (add to the 6 negatives):
    - `testFillPrevSelfAlias` — `FILL(PREV(a))` where `a` is target. Assert output matches bare `FILL(PREV)` for the same query. Internally the self-alias is normalized to `FILL_PREV_SELF` (D-06). Assert plan text shows `fill=prev` (not a column-specific annotation).
    - `testFillPrevAcceptPrevToSelfPrev` — `FILL(PREV(b), PREV)` where column `a` references `b`, and `b` is bare PREV (FILL_PREV_SELF). Chain rule: `fillModes[a]=b`, `fillModes[b]=FILL_PREV_SELF=-2`. `-2 < 0`, so chain rule does NOT fire. Accept.
    - `testFillPrevAcceptPrevToConstant` — `FILL(PREV(b), NULL)` and `FILL(PREV(b), 42.0)`. Source `b` is FILL_CONSTANT=-1. Chain rule does not fire. Accept. Both output forms tested in the same test.

    **Sub-task B — 5 FILL_KEY tests** in `SampleByFillTest.java`.

    For each, construct a keyed `SAMPLE BY FILL(NULL)` query with sparse data (gap between buckets so fill rows emit), and assert the fill rows contain the expected key value (not null).

    - `testFillKeyedUuid` — table `CREATE TABLE t (ts TIMESTAMP, u UUID, v DOUBLE)`, two rows with same UUID key and a 1-hour gap. Query: `SELECT ts, u, avg(v) FROM t SAMPLE BY 1h FILL(NULL)`. Assert the fill row for the gap bucket has the same UUID, not null. Exercises `FillRecord.getLong128Hi` and `getLong128Lo` FILL_KEY dispatch.
    - `testFillKeyedLong256` — key type `LONG256`. Similar structure. Exercises `getLong256A`, `getLong256B`, `getLong256(col, sink)`.
    - `testFillKeyedDecimal128` — key type `DECIMAL(25,2)`. Exercises `getDecimal128(col, sink)`.
    - `testFillKeyedDecimal256` — key type `DECIMAL(39,2)`. Exercises `getDecimal256(col, sink)`.
    - `testFillPrevGeoNoPrevYet` — key type `GEOHASH(4c)` (or similar 8-bit/16-bit/32-bit variant). Query with `FROM '2024-01-01' TO '2024-01-02'` and data only in later buckets — leading fill rows emit null sentinels. Assert the leading fill rows use `GeoHashes.*_NULL` (not `0`, not `Numbers.*_NULL`). Cover all four geo widths (8/16/32/64-bit) if possible in one test, or split into four tests. CONTEXT allows either — simplest is one test per geo width but a single consolidated test is acceptable per the "Covers the null-sentinel branches at lines 933, 945, 957, 969" language in CONTEXT item 12.

    All tests must use `assertMemoryLeak` wrapping and `assertQueryNoLeakCheck` for the data assertion (not `assertSql`) since they exercise the fast path with `supportsRandomAccess=false / size()=-1`.

    Do NOT add banner comments. Place all new tests in strict alphabetical slots.
  </action>
  <verify>
    <automated>mvn -pl core -Dtest='SampleByFillTest#testFillPrevReject*,SampleByFillTest#testFillPrevAccept*,SampleByFillTest#testFillPrevSelfAlias,SampleByFillTest#testFillKeyed*,SampleByFillTest#testFillPrevGeoNoPrevYet' test -q 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "public void testFillPrev(Reject|Accept)" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns at least `9` (6 Reject + 3 Accept/SelfAlias; count includes existing + new).
    - `grep -n "testFillPrevRejectTimestamp\|testFillPrevRejectMutualChain\|testFillPrevRejectThreeHopChain\|testFillPrevRejectFuncArg\|testFillPrevRejectMultiArg\|testFillPrevRejectNoArg\|testFillPrevRejectBindVar\|testFillPrevRejectTypeMismatch\|testFillPrevSelfAlias\|testFillPrevAcceptPrevToSelfPrev\|testFillPrevAcceptPrevToConstant" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns all 11 method declarations.
    - `grep -n "testFillKeyedUuid\|testFillKeyedLong256\|testFillKeyedDecimal128\|testFillKeyedDecimal256\|testFillPrevGeoNoPrevYet" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns 5 method declarations.
    - Each negative test uses `assertExceptionNoLeakCheck` (or equivalent `try { execute(query); fail(); } catch (SqlException e) { assertThat(e.getMessage(), containsString(...)); }`).
    - Each FILL_KEY test uses `assertQueryNoLeakCheck` with `supportsRandomAccess=false`, `expectSize=false`.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevReject*' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillKeyed*' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevGeoNoPrevYet' test` exits 0.
    - `mvn -pl core -Dtest='SampleByFillTest#testFillPrevSelfAlias' test` exits 0.
  </acceptance_criteria>
  <done>
    All grammar regression tests pass: positive (3) + negative (8 including 4 malformed-PREV shapes) = 11 total. All 5 FILL_KEY tests pass. Production fixes from phase 11 (128/256-bit FILL_KEY dispatch, geo null sentinels) now have regression coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: assertSql→assertQueryNoLeakCheck conversions + plan-text assertion refresh</name>
  <files>
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java,
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java,
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByNanoTimestampTest.java,
    core/src/test/java/io/questdb/test/griffin/ExplainPlanTest.java
  </files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — every `assertSql` call (currently 42 per research)
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — every `assertPlanNoLeakCheck` call that mentions "Sample By Fill" in the expected plan
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java — every `assertPlanNoLeakCheck` call that mentions "Sample By Fill"
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByNanoTimestampTest.java — every `assertPlanNoLeakCheck` call that mentions "Sample By Fill"
    - core/src/test/java/io/questdb/test/griffin/ExplainPlanTest.java — every test that exercises `SAMPLE BY ... FILL(...)` queries with plan assertions
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-02-SUMMARY.md — plan 02's output lists the tests that now fail due to the new `fill=` attribute; use that list as the starting point for task 3
    - .planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-CONTEXT.md item 13 (D-10 conversion rule) and item 20 (toPlan fill= unconditional)
  </read_first>
  <action>
    **Sub-task A — assertSql → assertQueryNoLeakCheck conversions** (CONTEXT item 13, D-10).

    For each `assertSql(expected, query)` call in `SampleByFillTest.java`, apply the D-10 rule:
    - **Convert** if: (1) the test exercises the new fast-path fill cursor (query contains `SAMPLE BY ... FILL(NULL)`, `FILL(PREV)`, or `FILL(<const>)` AND the query uses supported types only — i.e. NOT a query that will retro-fallback to legacy) AND (2) `supportsRandomAccess=false`, `size()=-1` are the correct expected values.
    - **Skip** if: the test asserts a legacy plan (plan contains `Sample By` without `Sample By Fill`), has mid-test DDL (e.g. `ALTER TABLE` between data inserts and the assertion), or is a cairo storage test (not applicable here — SampleByFillTest is in the griffin package).

    Conversion template:
    ```java
    // Before:
    assertSql(expected, query);

    // After — requires the expected-timestamp column name:
    assertQueryNoLeakCheck(expected, query, "ts", false, false);
    ```
    The `"ts"` is the column name of the designated timestamp in the expected output — for most SampleByFillTest tests this is `ts`. Verify per-test by checking the test's `CREATE TABLE ... TIMESTAMP(<name>)` setup.

    Expected count: ~15 conversions per CONTEXT (not a target — apply the rule mechanically). If the count comes out significantly different (e.g., 5 or 25), document the discrepancy in the summary.

    **Sub-task B — Plan-text assertion refresh** (CONTEXT item 20 — affects CONTEXT item 13's downstream).

    The new `fill=null|prev|value` attribute from plan 02 task 1 is emitted unconditionally in `toPlan`. Every `assertPlanNoLeakCheck` call whose expected plan contains `Sample By Fill` must now include the `fill=...` attribute line in the expected string at the correct position.

    Example transformation. Current (WRONG — will fail post plan 02):
    ```java
    assertPlanNoLeakCheck(
        "SELECT ts, avg(v) FROM t SAMPLE BY 1h FILL(NULL)",
        "Sort light\n" +
        "  keys: [ts]\n" +
        "    Sample By Fill\n" +
        "      stride='1h'\n" +
        "        Async Group By workers: 1\n" +
        "          ...\n"
    );
    ```
    Updated (CORRECT):
    ```java
    assertPlanNoLeakCheck(
        "SELECT ts, avg(v) FROM t SAMPLE BY 1h FILL(NULL)",
        "Sort light\n" +
        "  keys: [ts]\n" +
        "    Sample By Fill\n" +
        "      fill=null\n" +
        "      stride='1h'\n" +
        "        Async Group By workers: 1\n" +
        "          ...\n"
    );
    ```
    The exact placement of `fill=...` in the plan output depends on `toPlan`'s emission order. Per plan 02 task 1, the order is:
    1. `sink.type("Sample By Fill")` — the node type
    2. Optional `range=` (if FROM or TO are present)
    3. `stride=...`
    4. `fill=null|prev|value` — NEW from plan 02 (note: plan 02's action said fill goes after stride; confirm against the final code in SampleByFillRecordCursorFactory.java)
    5. `child(base)`

    Verify the exact order by reading `SampleByFillRecordCursorFactory.java:toPlan` after plan 02 lands, then update the test expected strings accordingly. The executor of this plan MUST verify the actual plan output (run one failing test, copy the actual output, use it as the new expected) rather than guess.

    Files to scan and update:
    1. `SampleByFillTest.java` — all `assertPlanNoLeakCheck` calls (5 per research).
    2. `SampleByTest.java` — any `assertPlanNoLeakCheck` that asserts against a `Sample By Fill` plan. Grep: `grep -n "Sample By Fill" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java`.
    3. `SampleByNanoTimestampTest.java` — mirrors of SampleByTest adjustments. Use the same pattern as plan 08 (phase 8) used for nano timestamp adjustments.
    4. `ExplainPlanTest.java` — any test that asserts `Sample By Fill` plan text. Grep: `grep -n "Sample By Fill" core/src/test/java/io/questdb/test/griffin/ExplainPlanTest.java`.

    For each failing assertion, the minimal viable update is to insert `"      fill=null\n"` (or `prev` / `value`) at the correct position in the expected string. Indentation follows the existing plan indentation convention.

    Do NOT add banner comments. Do NOT reorder unrelated tests.
  </action>
  <verify>
    <automated>mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test -q 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assertSql(" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` is strictly less than the pre-task count (conversions happened). Difference ~15 per CONTEXT estimate — document exact count in summary.
    - `grep -c "assertQueryNoLeakCheck(" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` is strictly greater than the pre-task count.
    - `grep -nE 'fill=null|fill=prev|fill=value' core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns at least 5 lines (one per assertPlanNoLeakCheck call).
    - `grep -n "Sample By Fill" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java` — every match's immediate context (grep -A 5) contains a `fill=...` attribute line.
    - Same for `SampleByNanoTimestampTest.java` and `ExplainPlanTest.java`.
    - `mvn -pl core -Dtest=SampleByFillTest test` exits 0.
    - `mvn -pl core -Dtest=SampleByTest test` exits 0.
    - `mvn -pl core -Dtest=SampleByNanoTimestampTest test` exits 0.
    - `mvn -pl core -Dtest=ExplainPlanTest test` exits 0.
    - Combined full suite: `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test` exits 0.
  </acceptance_criteria>
  <done>
    Phase 12's full intended test surface is green: ~15 conversions applied per D-10, plan-text assertions refreshed across 4 test files. `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production code | Tests exercise the production fallback, grammar, and FILL_KEY code paths. Test correctness is load-bearing for phase 12's correctness guarantees. |
| Test expectations → user observable behavior | Plan-text assertions are proxies for what users see in EXPLAIN output. Wrong expectations ship wrong behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-16 | Tampering / wrong test expectation | Retro-fallback test accidentally asserts fast-path plan (would hide a regression) | MEDIUM | mitigate | Each fallback test in task 1 explicitly asserts the plan contains `Sample By` (not `Sample By Fill`) — the wrong string would fail immediately. Acceptance criteria enforce the grep for `Sample By Fill` absent from those tests. |
| T-12-17 | Tampering / asymmetric conversion | assertSql → assertQueryNoLeakCheck conversion applied to a test where supportsRandomAccess=true is correct (false positive) | LOW | mitigate | D-10 rule is applied mechanically per the criteria: only convert when the test exercises the fast-path fill cursor AND the factory properties are `false/-1`. Summary documents the exact count — if it's off, the rule was misapplied. |
| T-12-18 | Denial of Service | Plan-text refresh introducing a plan attribute the actual code doesn't emit (test fails forever) | LOW | mitigate | Task 3 action requires "run one failing test, copy the actual output, use it as the new expected" — no guessing. Full-suite green is the acceptance criterion. |
| T-12-19 | Information disclosure | Grammar-reject test error message leaking internal tokens | LOW | accept | Error messages come from the user's own query text (column names, aliases) — already public. No internal state. |
</threat_model>

<verification>
- `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test` exits 0.
- All 19 new test methods exist (verified by `grep -cE "public void test(FillPrev|FillKeyed|FillToNullTimestamp)"` returning ≥ pre-task count + 19).
- `testSampleByFillNeedFix` no longer has the "two rows per bucket" comment.
- `grep -c "fill=null\|fill=prev\|fill=value"` across the four test files is non-zero.
</verification>

<success_criteria>
- [ ] 5 retro-fallback tests written, each asserts legacy plan + correct output.
- [ ] 1 testFillToNullTimestamp written, exercises hasExplicitTo LONG_NULL guard.
- [ ] testSampleByFillNeedFix restored to master's 3-row output; misleading comment deleted.
- [ ] 11 grammar tests written (3 positive + 8 negative covering D-05..D-09).
- [ ] 5 FILL_KEY tests written (UUID, Long256, Decimal128, Decimal256, geo-no-prev-yet).
- [ ] ~15 assertSql→assertQueryNoLeakCheck conversions applied in SampleByFillTest.java per D-10.
- [ ] Plan-text assertions refreshed for the new unconditional `fill=` attribute across SampleByFillTest, SampleByTest, SampleByNanoTimestampTest, ExplainPlanTest.
- [ ] Full suite green: `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test` exits 0.
- [ ] No banner comments added.
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-04-SUMMARY.md` documenting:
- Exact counts delivered: 5 retro-fallback tests, 11 grammar tests (clarify the delta vs CONTEXT's "8"), 5 FILL_KEY tests, 1 TO-null test, 1 restoration = 23 total.
- Number of assertSql→assertQueryNoLeakCheck conversions actually applied (compared to CONTEXT estimate of ~15).
- Complete list of plan-text assertion sites refreshed, grouped by file.
- Any surprises: assertion trips, unexpected failures, tests that needed creative construction (e.g., if bind-var case couldn't be reached through generateFill, document the alternative path).
- Confirmation that `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest' test` is green, with total runtime.
- Any Dates.parseOffset assertion trips observed (per plan 03 task 3 contingency) — if any, document the flip to SqlException.
</output>
