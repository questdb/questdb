---
phase: 13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi
plan: 04
type: execute
wave: 4
depends_on: [13-03-PLAN.md]
files_modified:
  - core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java
  - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
  - core/src/main/java/io/questdb/griffin/SqlOptimiser.java
  - core/src/main/java/io/questdb/griffin/model/QueryModel.java
  - core/src/main/java/io/questdb/griffin/model/IQueryModel.java
  - core/src/main/java/io/questdb/griffin/model/QueryModelWrapper.java
  - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
autonomous: true
requirements:
  - INTERNAL-REFACTOR-PH13
tags: [sql, fill, prev, retro-fallback, cleanup, deletion]

must_haves:
  truths:
    - "FallbackToLegacyException class is deleted from the codebase"
    - "QueryModel.stashedSampleByNode field + accessors are deleted"
    - "IQueryModel.getStashedSampleByNode / setStashedSampleByNode declarations are deleted"
    - "QueryModelWrapper.getStashedSampleByNode / setStashedSampleByNode delegations are deleted"
    - "SqlOptimiser.rewriteSampleBy no longer writes the stash"
    - "SqlCodeGenerator has zero references to FallbackToLegacyException — import gone, throw gone, three try/catch blocks gone, codegen-time unsupported-type check gone"
    - "isFastPathPrevSupportedType method is deleted (no remaining callers)"
    - "The phase-12 retro-fallback guard tests (7 methods listed below) are deleted from SampleByFillTest.java"
    - "Full test suite green: SampleByFillTest, SampleByTest, SampleByNanoTimestampTest, ExplainPlanTest, RecordCursorMemoryUsageTest all exit 0"
  artifacts:
    - path: core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java
      provides: "DELETED FILE — must not exist post-plan"
      contains: ""
  key_links:
    - from: "grep -r FallbackToLegacyException core/src/main/java"
      to: "zero matches"
      via: "complete deletion per D-04"
      pattern: "FallbackToLegacyException"
    - from: "grep -r stashedSampleByNode core/src/main/java"
      to: "zero matches"
      via: "field + accessors + wrapper + stash write all deleted"
      pattern: "stashedSampleByNode"
---

<objective>
Delete the retro-fallback machinery end-to-end per CONTEXT.md D-04. Plan 03 proved that the rowId rewrite unlocks every currently-retro-fallback-routed type; retro-fallback is now dead code. This plan removes the exception class, the QueryModel stash, the optimizer stash write, the codegen detection, the three try/catch sites, and the now-unused `isFastPathPrevSupportedType` method — plus the 7 retro-fallback guard tests in `SampleByFillTest.java`.

Purpose: Remove dead code and reduce SqlCodeGenerator complexity. The rowId mechanism makes the gate's "correctness backstop" role obsolete (per CONTEXT.md D-04: "delete the safety-net block at SqlCodeGenerator.java:3497-3512"). Reviewers can now see a simpler fill-gate path.

Output: Commit 4 of the phase-13 six-commit sequence (D-07). `FallbackToLegacyException.java` deleted. 5 files modified. 7 test methods deleted. Full phase suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-CONTEXT.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-03-SUMMARY.md
@.planning/phases/12-replace-safety-net-reclassification-with-legacy-fallback-and/12-CONTEXT.md

<interfaces>
Complete deletion surface per RESEARCH.md §3.1-3.8:

1. `core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java` — delete entire file.

2. `core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java`:
   - Line 70 (approx): `import io.questdb.griffin.FallbackToLegacyException;` — delete.
   - Lines 1200-1211 (approx): `isFastPathPrevSupportedType()` method — delete.
   - Lines 3524-3542 (approx): `prevSourceCols` build loop — delete.
   - Lines 3544-3557 (approx): codegen-time type check loop that throws `FallbackToLegacyException.INSTANCE` — delete.
   - Lines 8043-8056 (approx): first `catch (FallbackToLegacyException fallback)` block — delete catch; surrounding try must be removed or collapsed to plain `generateFill(...)` call.
   - Lines 8259-8272 (approx): second catch block — delete.
   - Lines 8307-8320 (approx): third catch block — delete.
   For each try/catch removal: if the outer `try` exists only to catch `FallbackToLegacyException`, remove the `try { ... }` wrapper too so the code reads as a direct `generateFill(...)` call.

3. `core/src/main/java/io/questdb/griffin/model/QueryModel.java`:
   - Line 185 (approx): `private ExpressionNode stashedSampleByNode;` — delete.
   - Line 471 (approx): `stashedSampleByNode = null;` inside `clear()` — delete.
   - Line 1066 (approx): `getStashedSampleByNode()` getter — delete.
   - Lines 1855-1856 (approx): `setStashedSampleByNode(...)` setter — delete.
   - Any `copy()` / equivalent methods that propagate the field — delete the relevant lines.

4. `core/src/main/java/io/questdb/griffin/model/IQueryModel.java`:
   - Line 403 (approx): `ExpressionNode getStashedSampleByNode();` — delete.
   - Line 639 (approx): `void setStashedSampleByNode(ExpressionNode node);` — delete.

5. `core/src/main/java/io/questdb/griffin/model/QueryModelWrapper.java`:
   - Line 645-646 (approx): `getStashedSampleByNode()` delegating — delete.
   - Line 1246 (approx): `setStashedSampleByNode(...)` throwing `UnsupportedOperationException` — delete.

6. `core/src/main/java/io/questdb/griffin/SqlOptimiser.java`:
   - Line 8464 (approx): `nested.setStashedSampleByNode(sampleBy);` — delete.

7. `core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java`:
   - If the factory holds a `prevSourceCols` IntList parameter used for snapshotting per-column sources: replace with `hasPrevFill` derived from `fillModes` scan (RESEARCH.md §3.8). The constructor parameter can be dropped OR kept as `null`/empty-list with `hasPrevFill` sourced differently. Planner/executor's call per D-04 discretion.
   - Ensure `toPlan()` still emits `fill=` attribute correctly; `hasPrevFill` drives the choice of `fill=prev` vs `fill=null` vs `fill=value`.

8. `core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java`:
   Delete these 7 test methods:
   - `testFillPrevSymbolLegacyFallback` (line ~1796 per RESEARCH.md §3.7)
   - `testFillPrevCrossColumnUnsupportedFallback` (line ~1044)
   - `testFillPrevDecimal128LegacyFallback`
   - `testFillPrevDecimal256LegacyFallback`
   - `testFillPrevLong256LegacyFallback`
   - `testFillPrevUuidLegacyFallback`
   - `testFillPrevLong128LegacyFallback`
   Locate exact line numbers via `grep -n "public void testFillPrev\(SymbolLegacyFallback\|CrossColumnUnsupportedFallback\|Decimal128LegacyFallback\|Decimal256LegacyFallback\|Long256LegacyFallback\|UuidLegacyFallback\|Long128LegacyFallback\)" <file>`.

**Deliberately out of scope in this plan (Plan 05 and Plan 06 handle):**
- `testSampleByFillNeedFix` restoration (Plan 05).
- Defect 1 CTE/outer-projection post-rewrite disposition (Plan 05).
- WR-04 precise chain-rejection position (Plan 06).
- Defect 3 insufficient-fill grammar + `testFillInsufficientFillValues` (Plan 06).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete retro-fallback machinery across 6 production files and delete 7 retro-fallback tests</name>
  <files>core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java, core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java, core/src/main/java/io/questdb/griffin/SqlOptimiser.java, core/src/main/java/io/questdb/griffin/model/QueryModel.java, core/src/main/java/io/questdb/griffin/model/IQueryModel.java, core/src/main/java/io/questdb/griffin/model/QueryModelWrapper.java, core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java, core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java</files>
  <read_first>
    - core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java (current file to delete)
    - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java — specifically the sections at approximate lines 70, 1200-1211, 3524-3557, 8043-8056, 8259-8272, 8307-8320 (use grep to locate exact lines)
    - core/src/main/java/io/questdb/griffin/model/QueryModel.java — lines around `stashedSampleByNode`
    - core/src/main/java/io/questdb/griffin/model/IQueryModel.java — lines around `getStashedSampleByNode` / `setStashedSampleByNode`
    - core/src/main/java/io/questdb/griffin/model/QueryModelWrapper.java — delegation and wrapper throw
    - core/src/main/java/io/questdb/griffin/SqlOptimiser.java — the `rewriteSampleBy` stash write
    - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java — `prevSourceCols` usage + `toPlan` `fill=` attribute logic
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java — locate the 7 tests by exact name
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md §3.1-3.8 (deletion surface)
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-CONTEXT.md D-04
  </read_first>
  <action>
    Step 1 — Locate exact line numbers for every surgical deletion. Do NOT rely on the approximate line numbers in RESEARCH.md; they may be stale after Plan 02's edits. Use grep to resolve:
    ```bash
    grep -n "FallbackToLegacyException" core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
    grep -n "stashedSampleByNode" core/src/main/java/io/questdb/griffin/model/QueryModel.java
    grep -n "stashedSampleByNode" core/src/main/java/io/questdb/griffin/model/IQueryModel.java
    grep -n "stashedSampleByNode" core/src/main/java/io/questdb/griffin/model/QueryModelWrapper.java
    grep -n "stashedSampleByNode\|setStashedSampleByNode" core/src/main/java/io/questdb/griffin/SqlOptimiser.java
    grep -n "isFastPathPrevSupportedType\|prevSourceCols" core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
    grep -n "public void testFillPrev\(SymbolLegacyFallback\|CrossColumnUnsupportedFallback\|Decimal128LegacyFallback\|Decimal256LegacyFallback\|Long256LegacyFallback\|UuidLegacyFallback\|Long128LegacyFallback\)" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
    ```

    Step 2 — Delete file `core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java` entirely. Use `rm core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java` or `git rm`.

    Step 3 — `SqlCodeGenerator.java`:
    - Delete `import io.questdb.griffin.FallbackToLegacyException;`.
    - Delete the entire `isFastPathPrevSupportedType()` method (including any preceding @Override / comment blocks).
    - In `generateFill`, delete the `prevSourceCols` build loop AND the codegen-time type-check loop that throws `FallbackToLegacyException.INSTANCE`. After Plan 02, `prevSourceCols` may still be constructed for `hasPrevFill` detection — verify whether any code outside `generateFill` reads `prevSourceCols` (grep to confirm). If the factory constructor still takes a `prevSourceCols` argument, update the call site to pass `null` or an empty `IntList`. See step 7 for the matching factory-side change.
    - For each of the three `catch (FallbackToLegacyException fallback)` blocks in `generateSelectGroupBy` (3 call sites of `generateFill`), remove the catch. If the surrounding `try` contains ONLY that catch, remove the `try { ... }` wrapper too, leaving the direct `generateFill(...)` call. If the `try` has other catches (unlikely per RESEARCH.md but verify), keep the try and remove only the `FallbackToLegacyException` catch.

    After SqlCodeGenerator edits, confirm:
    ```bash
    grep -c "FallbackToLegacyException" core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
    ```
    must return 0.

    Step 4 — `QueryModel.java`:
    - Delete the field declaration `private ExpressionNode stashedSampleByNode;`.
    - Delete the reset `stashedSampleByNode = null;` inside `clear()`.
    - Delete the getter `public ExpressionNode getStashedSampleByNode()` (and its `@Override` if present).
    - Delete the setter `public void setStashedSampleByNode(ExpressionNode ...)` (and its `@Override` if present).
    - If `copyNonPrimaryFields` / `copyBounds` / similar propagation methods reference the field, delete those lines.

    Step 5 — `IQueryModel.java`:
    - Delete the interface declarations `ExpressionNode getStashedSampleByNode();` and `void setStashedSampleByNode(ExpressionNode node);`.

    Step 6 — `QueryModelWrapper.java`:
    - Delete the `getStashedSampleByNode()` delegation method.
    - Delete the `setStashedSampleByNode(...)` method that throws `UnsupportedOperationException`.

    Step 7 — `SqlOptimiser.java`:
    - In `rewriteSampleBy`, delete the stash write line (approximately `nested.setStashedSampleByNode(sampleBy);`).

    Step 8 — `SampleByFillRecordCursorFactory.java`:
    - If the constructor still takes a `prevSourceCols` `IntList` parameter that was only used for snapshotting per-column sources (dead under rowId), choose one of:
      (a) Remove the parameter entirely and update `generateFill`'s call site to match.
      (b) Keep the parameter and derive `hasPrevFill` from a `fillModes` scan inside the constructor body. This preserves the current parameter list for lower blast radius.
    - Ensure `toPlan()` still emits the correct `fill=null|prev|value` attribute (phase 12 locked this as unconditional). `hasPrevFill` must be set correctly when ANY column has `FILL_PREV_SELF` OR a cross-column PREV mode (`mode >= 0`). A simple scan:
      ```java
      boolean hasPrevFill = false;
      for (int i = 0, n = fillModes.size(); i < n; i++) {
          int m = fillModes.getQuick(i);
          if (m == FILL_PREV_SELF || m >= 0) { hasPrevFill = true; break; }
          // treat FILL_KEY (-3) and FILL_CONSTANT (-1 or similar) as non-prev
      }
      ```
      Adjust to match the actual `FILL_*` constants in the file.

    Step 9 — `SampleByFillTest.java`:
    - Delete the 7 test methods (including each `@Test` annotation and method body):
      - `testFillPrevSymbolLegacyFallback`
      - `testFillPrevCrossColumnUnsupportedFallback`
      - `testFillPrevDecimal128LegacyFallback`
      - `testFillPrevDecimal256LegacyFallback`
      - `testFillPrevLong256LegacyFallback`
      - `testFillPrevUuidLegacyFallback`
      - `testFillPrevLong128LegacyFallback`
    - Remove any helper methods used ONLY by these tests.

    Step 10 — Build and test:
    ```bash
    mvn -pl core -DskipTests -P local-client package 2>&1 | tail -5
    ```
    Expect `BUILD SUCCESS`. If compile errors remain, they point to missed deletion sites — resolve them before running tests.

    ```bash
    mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test 2>&1 | tail -30
    ```
    Expect `BUILD SUCCESS` with zero failures / zero errors across all 5 suites.

    Step 11 — Commit. This is Commit 4 of phase 13 per D-07. Commit title (short, plain English): e.g. `Delete retro-fallback machinery`. Body must state:
    - Rationale: Plan 03 proved the rowId rewrite unlocks every currently-retro-fallback-routed type.
    - Scope: `FallbackToLegacyException` class deleted; `stashedSampleByNode` field+accessors+wrapper delegations removed; `SqlOptimiser.rewriteSampleBy` stash write removed; `SqlCodeGenerator` codegen detection + 3 try/catch sites + `isFastPathPrevSupportedType` method removed; 7 retro-fallback guard tests deleted.
    - Reference: CONTEXT.md D-04, phase-13 Commit 4 of 6.
  </action>
  <verify>
    <automated>test ! -f core/src/main/java/io/questdb/griffin/FallbackToLegacyException.java &amp;&amp; mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test 2&gt;&amp;1 | tail -10 | grep -q 'BUILD SUCCESS'</automated>
  </verify>
  <acceptance_criteria>
    - `find core/src/main/java/io/questdb/griffin -name FallbackToLegacyException.java` returns empty output (file gone).
    - `grep -rn "FallbackToLegacyException" core/src/main/java core/src/test/java` returns zero matches.
    - `grep -rn "stashedSampleByNode" core/src/main/java core/src/test/java` returns zero matches.
    - `grep -rn "getStashedSampleByNode\|setStashedSampleByNode" core/src/main/java core/src/test/java` returns zero matches.
    - `grep -n "isFastPathPrevSupportedType" core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java` returns zero matches.
    - `grep -c "public void testFillPrev\(SymbolLegacyFallback\|CrossColumnUnsupportedFallback\|Decimal128LegacyFallback\|Decimal256LegacyFallback\|Long256LegacyFallback\|UuidLegacyFallback\|Long128LegacyFallback\)" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns 0.
    - `mvn -pl core -DskipTests -P local-client package 2>&1 | tail -5` contains `BUILD SUCCESS`.
    - `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test 2>&1 | tail -30` contains `BUILD SUCCESS` and zero failures/errors in the final summary.
    - The 13 cherry-picked tests from Plan 03 are still present: `grep -c "public void testFillPrev\(SymbolKeyed\|SymbolNonKeyed\|SymbolNull\|ArrayDouble1D\|StringKeyed\|StringNonKeyed\|VarcharKeyed\|VarcharNonKeyed\|UuidKeyed\|UuidNonKeyed\|Long256NonKeyed\|Decimal128\|Decimal256\)" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns 13.
    - `toPlan` still emits `fill=` attribute: `grep -n "fill=" core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java` returns at least 1 match.
    - No banner comments introduced: `git diff HEAD~1 HEAD -- core/src/main/java core/src/test/java | grep -E "^\+.*// (===|---)"` returns no matches.
    - `git log -1 --pretty=%s` matches a plain-English short title (no Conventional Commits prefix).
  </acceptance_criteria>
  <done>
    Retro-fallback machinery deleted end-to-end. `FallbackToLegacyException.java` gone. Zero references to `stashedSampleByNode` anywhere. `isFastPathPrevSupportedType` gone. Three try/catch sites simplified. 7 retro-fallback guard tests deleted. The 13 Plan-03 per-type tests still pass. Full phase suite green. Commit 4 of phase 13 in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal refactor — no new auth boundary, no new user input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Tampering | Incomplete deletion leaves dangling references to deleted field/class, causing runtime NPE or ClassNotFoundException | mitigate | Acceptance grep for `FallbackToLegacyException` and `stashedSampleByNode` across both `core/src/main/java` and `core/src/test/java` returns zero matches. Compile must succeed. |
| T-13-04-02 | Denial of Service | `toPlan()` emits missing `fill=` attribute after the prevSourceCols refactor, breaking ExplainPlanTest | mitigate | Acceptance criterion verifies `ExplainPlanTest` passes. Plan-text helper in Plan 03 tests also covers this via `Sample By Fill` assertions. |
| T-13-04-03 | Elevation of Privilege | Retro-fallback tests retained for types that actually DON'T fast-path, hiding regressions | accept | Plan 03 proved every currently-retro-fallback-routed type now fast-paths. Accept that deleting these tests removes a safety net, compensated by the 13 new per-type tests which assert `Sample By Fill` for the same types. |
</threat_model>

<verification>
- `FallbackToLegacyException.java` file does not exist.
- Zero grep matches for `FallbackToLegacyException` or `stashedSampleByNode` in `core/src/main/java` and `core/src/test/java`.
- All 5 listed test suites pass.
- 13 cherry-picked tests from Plan 03 still pass post-deletion.
- `ExplainPlanTest` passes (verifies `fill=` attribute emission is still correct).
</verification>

<success_criteria>
- Commit 4 of phase 13 landed with retro-fallback deletion.
- Zero dangling references across main + test code.
- Full phase test surface green.
- 13 per-type tests still fast-path.
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-04-SUMMARY.md` documenting:
- Files deleted (1) and files modified (7).
- Line counts removed per file.
- Test methods deleted (7).
- Test suite results (all 5 suites).
- Confirmation that Plan 05 is unblocked.
</output>
