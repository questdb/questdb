---
phase: 13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi
plan: 03
type: execute
wave: 3
depends_on: [13-02-PLAN.md]
files_modified:
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
autonomous: true
requirements:
  - INTERNAL-REFACTOR-PH13
tags: [sql, fill, prev, test, cherry-pick, symbol, string, varchar, uuid, long256, decimal128, decimal256, array]

must_haves:
  truths:
    - "All 13 per-type FILL(PREV) tests from commit f43a3d7057 land in SampleByFillTest.java on this branch"
    - "All 13 tests pass on the fast path — plan output contains 'Sample By Fill' for every type, not 'Sample By'"
    - "No existing SampleByFillTest method is modified by the cherry-pick"
    - "The commit is independently bisectable: before it, the 13 tests do not exist; after it, they all pass with zero failures"
  artifacts:
    - path: core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
      provides: "13 new @Test methods for SYMBOL (3) + ARRAY (1) + STRING (2) + VARCHAR (2) + UUID (2) + Long256 (1) + Decimal128 (1) + Decimal256 (1) per-type FILL(PREV)"
      contains: "testFillPrevSymbolKeyed"
  key_links:
    - from: "each new test method"
      to: "an assertion that the plan output contains 'Sample By Fill'"
      via: "assertPlanNoLeakCheck or inline plan grep"
      pattern: "Sample By Fill"
---

<objective>
Cherry-pick the 13 per-type FILL(PREV) tests from `sm_fill_prev_fast_all_types` commit `f43a3d7057` into our branch's `SampleByFillTest.java`. All 13 must pass on the fast path after Plan 02's rowId rewrite — plan output shows `Sample By Fill`, not `Sample By`. This is the validation evidence that the rowId rewrite unlocks every currently-retro-fallback-routed type, and the gate that authorizes Plan 04 (retro-fallback deletion).

Purpose: Per CONTEXT.md D-04, retro-fallback machinery is deleted ONLY IF the rowId rewrite unlocks every currently-unsupported type. This plan is the proof. If any of the 13 tests fail, Plan 04 pauses and the failure is triaged before retro-fallback comes out.

Output: Commit 3 of the phase-13 six-commit sequence (D-07). 13 new `@Test` methods in `SampleByFillTest.java`. All pass. Plan 04 is unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-CONTEXT.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-VALIDATION-INPUTS.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-02-SUMMARY.md

<interfaces>
Source commit: `f43a3d7057` on branch `sm_fill_prev_fast_all_types`.
Source file: `core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java`.
Fetch via: `git show f43a3d7057:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java`.

The 13 method names (complete list per VALIDATION-INPUTS.md and RESEARCH.md §6.2):
- `testFillPrevSymbolKeyed`
- `testFillPrevSymbolNonKeyed`
- `testFillPrevSymbolNull`
- `testFillPrevArrayDouble1D`
- `testFillPrevStringKeyed`
- `testFillPrevStringNonKeyed`
- `testFillPrevVarcharKeyed`
- `testFillPrevVarcharNonKeyed`
- `testFillPrevUuidKeyed`
- `testFillPrevUuidNonKeyed`
- `testFillPrevLong256NonKeyed`
- `testFillPrevDecimal128`
- `testFillPrevDecimal256`

Do NOT port the source commit's deletions. Source `f43a3d7057` deletes `testFillPrevSymbolLegacyFallback` and `testFillPrevCrossColumnUnsupportedFallback` — we KEEP those on our branch until Plan 04 ships. Per VALIDATION-INPUTS.md: "Tests to NOT borrow (deliberately retained on our branch)".

Plan-output expectation: every test must exercise the fast path. If the source tests don't already assert on the plan text, ADD an assertion that the plan contains `Sample By Fill`. If the source uses `assertSql` (no plan check), wrap a preliminary `assertPlanNoLeakCheck` call or switch to `assertQueryNoLeakCheck` where appropriate per project convention.

Project conventions for new tests:
- Use `assertMemoryLeak(() -> { ... })` wrapping.
- Prefer `assertQueryNoLeakCheck` for query correctness; `assertSql` only for storage assertions.
- SQL keywords UPPERCASE (or mixed case acceptable).
- Use `underscore_thousands` in numeric literals with 5+ digits.
- Multiline strings for longer SQL and for asserting multiline results.
- Use `execute()` for DDL.
- Insert `@Test` methods in correct alphabetical position among existing methods per project guideline (Java members sorted alphabetically within visibility group).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cherry-pick 13 per-type FILL(PREV) test methods from f43a3d7057 into SampleByFillTest.java and assert fast-path plan output</name>
  <files>core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java</files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java (current file, to identify insertion points and existing patterns)
    - Commit `f43a3d7057` via: `git show f43a3d7057:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java > /tmp/f43a3d7057-SampleByFillTest.java` — read the full file to locate each of the 13 method bodies
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-VALIDATION-INPUTS.md (full 13-test list with categories)
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md §6.4 (per-type notes for SYMBOL/STRING/VARCHAR/BIN/ARRAY semantics under rowId)
    - CLAUDE.md (test conventions: assertMemoryLeak, UPPERCASE SQL, underscore thousands, assertQueryNoLeakCheck vs assertSql, alphabetical member ordering)
  </read_first>
  <action>
    Step 1 — Extract the 13 method bodies from `f43a3d7057`. Dump the source file:
    ```bash
    git show f43a3d7057:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java > /tmp/f43a3d7057-SampleByFillTest.java
    ```
    Confirm all 13 exist in the dump:
    ```bash
    grep -E "public void (testFillPrevSymbolKeyed|testFillPrevSymbolNonKeyed|testFillPrevSymbolNull|testFillPrevArrayDouble1D|testFillPrevStringKeyed|testFillPrevStringNonKeyed|testFillPrevVarcharKeyed|testFillPrevVarcharNonKeyed|testFillPrevUuidKeyed|testFillPrevUuidNonKeyed|testFillPrevLong256NonKeyed|testFillPrevDecimal128|testFillPrevDecimal256)\(" /tmp/f43a3d7057-SampleByFillTest.java | wc -l
    ```
    Expected output: `13`. If not, stop and escalate.

    Step 2 — For each of the 13 methods, copy the method body (including any leading `@Test` annotation and preceding line comment if present) and insert into `core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` at the correct alphabetical position among existing `@Test` methods.

    Step 3 — For each inserted method, ensure the body contains at least one plan-text assertion that the fast path is selected. If the source uses:
    - `assertQueryNoLeakCheck(expected, query, timestampCol, expectSize)` — this already asserts factory properties; additionally add a `assertPlanNoLeakCheck(query, "Sample By Fill")` or equivalent containing-assertion just before the query result assertion.
    - `assertSql(expected, query)` — add a `assertPlanNoLeakCheck(query, ...)` beforehand.

    The exact helper method name depends on the existing test file's conventions — grep for `assertPlan` in the current `SampleByFillTest.java` to find the helper already in use. If there's no preexisting plan-text helper, use a pattern matching the existing tests that verify fast-path vs legacy (look at how the current `testFillPrevSymbolLegacyFallback` asserts `Sample By`).

    Minimum requirement: each of the 13 new tests must include a line containing the string literal `"Sample By Fill"` as part of a plan-text assertion. This is what the acceptance criterion grep-counts.

    Step 4 — Do NOT delete any existing tests. Specifically, retain:
    - `testFillPrevSymbolLegacyFallback` (asserts retro-fallback plan for SYMBOL first()).
    - `testFillPrevCrossColumnUnsupportedFallback` (asserts retro-fallback for unsupported cross-column PREV).
    - The 5 phase-12 retro-fallback guard tests (`testFillPrevDecimal128LegacyFallback`, `testFillPrevDecimal256LegacyFallback`, `testFillPrevLong256LegacyFallback`, `testFillPrevUuidLegacyFallback`, `testFillPrevLong128LegacyFallback`).
    These are Plan 04's responsibility to delete.

    Step 5 — Expect that in the cherry-picked bodies, the `expected` string for SYMBOL tests now shows carried-forward prev symbol values (rather than NULL under retro-fallback). The data values are semantic correctness baselines from the source branch and should pass identically under rowId because rowId reads the same chain-stored aggregate row.

    Step 6 — Build and run:
    ```bash
    mvn -pl core -DskipTests -P local-client package 2>&1 | tail -5
    ```
    Expect `BUILD SUCCESS`. Then:
    ```bash
    mvn -pl core -Dtest=SampleByFillTest test 2>&1 | tail -25
    ```
    Expect `BUILD SUCCESS`. The `Tests run:` count should be higher than before by exactly 13. All 13 new tests pass.

    Step 7 — Run each of the 13 tests individually to confirm per-test pass (useful for diagnosing any single-test failure):
    ```bash
    for t in testFillPrevSymbolKeyed testFillPrevSymbolNonKeyed testFillPrevSymbolNull testFillPrevArrayDouble1D testFillPrevStringKeyed testFillPrevStringNonKeyed testFillPrevVarcharKeyed testFillPrevVarcharNonKeyed testFillPrevUuidKeyed testFillPrevUuidNonKeyed testFillPrevLong256NonKeyed testFillPrevDecimal128 testFillPrevDecimal256; do
        mvn -pl core -Dtest=SampleByFillTest#$t test 2>&1 | tail -5 | grep -E 'BUILD SUCCESS|Tests run'
    done
    ```
    Each line should show `BUILD SUCCESS`.

    Step 8 — If ANY of the 13 tests fails:
    - STOP. Do not commit.
    - Capture the failing assertion output.
    - If the failure is a plan-text mismatch (expected `Sample By Fill`, got `Sample By`), then retro-fallback is still firing for that type. Investigate:
      - Plan 02 may have left the codegen unsupported-type check active (it's intentional — Plan 04 removes it). Confirm retro-fallback is still wrapping this query. If so, expected fix: delete the `isFastPathPrevSupportedType`/`prevSourceCols` type-check loop guard for that type EARLIER — bring that slice of Plan 04 forward.
      - OR add a `isFastPathPrevSupportedType` entry for the affected type if the rowId rewrite did not fully cover it.
    - If the failure is a value mismatch (wrong data, right plan), the rowId read path has a bug — debug in `SampleByFillRecordCursorFactory.FillRecord.getXxx(col)` or the emit-path `recordAt` call. This is a Plan 02 bug; escalate.
    - Either way: surface to the user before proceeding.

    Step 9 — Commit. Commit title (short, plain English, no Conventional Commits prefix): e.g. `Add per-type FILL(PREV) tests for all 10 types`. Body (≤72 chars/line) must state:
    - 13 tests cherry-picked from `sm_fill_prev_fast_all_types` commit `f43a3d7057`.
    - Tests cover SYMBOL (3), ARRAY (1), STRING (2), VARCHAR (2), UUID (2), Long256 (1), Decimal128 (1), Decimal256 (1).
    - All execute on the fast path (`Sample By Fill` in plan output).
    - This is Commit 3 of phase 13 (D-07); it unblocks Plan 04's retro-fallback deletion.
    - The phase-12 retro-fallback guard tests are intentionally retained until Plan 04.
  </action>
  <verify>
    <automated>mvn -pl core -Dtest=SampleByFillTest test 2&gt;&amp;1 | tail -10 | grep -q 'BUILD SUCCESS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public void testFillPrev\(SymbolKeyed\|SymbolNonKeyed\|SymbolNull\|ArrayDouble1D\|StringKeyed\|StringNonKeyed\|VarcharKeyed\|VarcharNonKeyed\|UuidKeyed\|UuidNonKeyed\|Long256NonKeyed\|Decimal128\|Decimal256\)\b" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns exactly 13.
    - `grep -c "testFillPrevSymbolLegacyFallback\|testFillPrevCrossColumnUnsupportedFallback\|testFillPrevDecimal128LegacyFallback\|testFillPrevDecimal256LegacyFallback\|testFillPrevLong256LegacyFallback\|testFillPrevUuidLegacyFallback\|testFillPrevLong128LegacyFallback" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java` returns >= 7 (none of the phase-12 retained tests deleted).
    - Each new test asserts fast-path: for each name in the 13-list, `grep -A200 "public void $name(" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java | head -200 | grep -c "Sample By Fill"` returns >= 1.
    - `mvn -pl core -Dtest=SampleByFillTest test 2>&1 | tail -10` contains `BUILD SUCCESS` and a `Tests run:` line with zero failures and zero errors.
    - Individual per-test runs pass for all 13: `for t in testFillPrevSymbolKeyed testFillPrevSymbolNonKeyed testFillPrevSymbolNull testFillPrevArrayDouble1D testFillPrevStringKeyed testFillPrevStringNonKeyed testFillPrevVarcharKeyed testFillPrevVarcharNonKeyed testFillPrevUuidKeyed testFillPrevUuidNonKeyed testFillPrevLong256NonKeyed testFillPrevDecimal128 testFillPrevDecimal256; do mvn -pl core -Dtest=SampleByFillTest#$t test 2>&1 | tail -3 | grep -q 'BUILD SUCCESS' || echo "FAIL: $t"; done` produces no `FAIL:` lines.
    - No banner comments introduced: `git diff HEAD -- core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java | grep -E "^\+.*// (===|---)"` returns no matches.
    - `git log -1 --pretty=%s` matches a plain-English short title (no `fix(...)`, `test(...)`, etc. Conventional Commits prefix).
  </acceptance_criteria>
  <done>
    13 per-type FILL(PREV) tests live in `SampleByFillTest.java` with fast-path plan assertions. All pass individually and in the suite. No existing phase-12 retro-fallback guard tests were deleted. Commit 3 of phase 13 is in place. Plan 04 authorized.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal test additions — no new production code, no new auth or input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | Plan 02's rowId rewrite silently falls back to legacy for some type | mitigate | Every new test asserts `Sample By Fill` in plan output. Acceptance criterion grep-counts the assertion across all 13 tests. |
| T-13-03-02 | Information Disclosure | Wrong chain-stored value leaks across buckets for variable-width types (STRING/VARCHAR/ARRAY) | mitigate | 13 type-specific tests include STRING, VARCHAR, ARRAY coverage with value assertions. Semantic correctness verified against source branch's baseline. |
| T-13-03-03 | Repudiation | Retro-fallback tests deleted prematurely, hiding regressions | accept | Acceptance criterion verifies retro-fallback guard tests remain. Plan 04 owns their deletion. |
</threat_model>

<verification>
- `mvn -pl core -Dtest=SampleByFillTest test` exits 0 with tests-run count ≥ previous count + 13.
- 13 new method names present in file.
- 7 retained retro-fallback guard tests still present.
- Each new test contains a plan-text assertion with `Sample By Fill`.
</verification>

<success_criteria>
- Commit 3 of phase 13 landed.
- 13 new `@Test` methods in `SampleByFillTest.java`, one per cherry-picked source method.
- All 13 pass on the fast path.
- Retro-fallback guard tests retained for Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-03-SUMMARY.md` documenting:
- The 13 test names and their categories.
- `mvn` test output summary (tests run, failures, errors).
- Per-test plan-text assertion used (helper method name and literal).
- Confirmation that Plan 04 is authorized to delete retro-fallback machinery.
</output>
