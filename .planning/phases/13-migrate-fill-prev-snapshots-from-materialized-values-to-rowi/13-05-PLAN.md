---
phase: 13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi
plan: 05
type: execute
wave: 5
depends_on: [13-04-PLAN.md]
files_modified:
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
  - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
  - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
autonomous: false
requirements:
  - INTERNAL-REFACTOR-PH13
tags: [sql, fill, prev, defect, seed-002, toTop, cte, outer-projection]

must_haves:
  truths:
    - "SampleByTest#testSampleByFillNeedFix is restored to master's 3-row form in both assertion #1 (assertQuery with sizeExpected=true) and assertion #2 (CTE + outer projection — assertQuery with 3 rows, not assertSql with 6 rows)"
    - "Running testSampleByFillNeedFix twice back-to-back (proving toTop() state fully resets) produces identical output"
    - "If Defect 1 (CTE+outer-projection corruption) is absorbed by the rowId rewrite — assertion #2 passes without any SqlCodeGenerator/generateFill plumbing change"
    - "If Defect 1 survives the rewrite — a factory-plumbing fix in generateFill() or SampleByFillRecordCursorFactory is applied in this plan per D-06 ('end of this phase should be a full solution') and assertion #2 passes"
    - "Plan text for assertion #2 contains 'Sample By Fill' (verifying fill cursor runs, not bypassed via legacy path)"
  artifacts:
    - path: core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
      provides: "testSampleByFillNeedFix in master's 3-row form, assertQuery-based for both assertion #1 and #2"
      contains: "testSampleByFillNeedFix"
  key_links:
    - from: "testSampleByFillNeedFix assertion #1"
      to: "sizeExpected=true double-iteration verifies toTop() correctness"
      via: "assertQuery(..., true) call"
      pattern: "assertQuery.*true\\s*\\)"
    - from: "testSampleByFillNeedFix assertion #2"
      to: "assertQuery (not assertSql) with 3 expected rows"
      via: "master's form restored"
      pattern: "assertQuery"
---

<objective>
Restore `SampleByTest#testSampleByFillNeedFix` to master's 3-row form in both assertions (#1 and #2), closing Phase 12 Success Criterion #1 deferred via Option A. This plan resolves SEED-002 Defects 1 and 2 — Defect 2 (toTop() state corruption) is expected to be absorbed by the rowId rewrite (RESEARCH.md §5.2); Defect 1 (CTE+outer-projection corruption) is investigated post-rewrite and fixed in-phase per D-06 if it survives.

Purpose: Per CONTEXT.md D-06 ("option 1 but fully fix it — end of this phase should be a full solution"), both defects must be resolved before phase 13 closes. This is the cursor/plumbing hygiene commit.

Output: Commit 5 of the phase-13 six-commit sequence (D-07). `testSampleByFillNeedFix` matches master. If Defect 1 requires a factory-plumbing fix, it lands in this commit.

Task 2 is `autonomous: false`: if assertion #2 still produces corrupted output after the rowId rewrite (Defect 1 not absorbed), the fix requires an architectural decision about where to patch (`generateFill()` timestamp-index lookup, outer-projection metadata threading, or similar). That decision surfaces to the user before the fix lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-CONTEXT.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md
@.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-04-SUMMARY.md
@.planning/seeds/SEED-002-phase12-cursor-defects-rowid-dependent.md

<interfaces>
Current state of `testSampleByFillNeedFix` (SampleByTest.java:5943, per RESEARCH.md §5.1 and SEED-002):
- **Assertion #1** (~line 5972): `assertQuery` with 3 expected rows on the current branch, BUT the current branch produces corrupted output per Defect 2. Test is in a workaround state (comment at line 6018 documents "two rows per bucket for the gap hour").
- **Assertion #2** (~line 6022): `assertSql` (not `assertQuery`) with 6 expected rows. CTE + outer projection form. This is the Defect-1 workaround.
- **Assertion #3** (~line 6057): `assertSql` accepting the insufficient-fill query without error. Defect-3 workaround — RESOLVED in Plan 06, not here.

Master's form (per RESEARCH.md §5.1):
- **Assertion #1**: `assertQuery` with 3 rows (including `cnt=0` for the gap hour — the correct PREV behavior). `high` column for gap row uses `case when cnt=0 then close else high end` = `0.00128`.
- **Assertion #2**: `assertQuery` (NOT `assertSql`) with 3 rows, `cnt=0` for gap.
- **Assertion #3**: `assertException` at position 554, "not enough fill values". Restored in Plan 06.

To view master's exact expected text:
```bash
git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java > /tmp/master-SampleByTest.java
grep -n "testSampleByFillNeedFix" /tmp/master-SampleByTest.java
# read the method body from /tmp/master-SampleByTest.java using the line numbers above
```

Defect-1 absorption hypothesis (RESEARCH.md §5.3):
- Wrong output = timestamps = `1970-01-01T00:00:00`, duplicated rows per bucket.
- Root cause candidate A: `generateFill()` at ~line 3361-3364 returns `groupByFactory` unchanged when `timestampIndex < 0` in outer metadata. Outer CTE projection may expose a different metadata shape to `generateFill`, causing the fill cursor to NEVER run — the outer ORDER BY ts DESC then re-sorts raw GROUP BY output, producing wrong timestamps.
- Root cause candidate B: Fill cursor runs but `FillRecord` column index mapping is wrong under outer-projection remap.
- Diagnostic via `EXPLAIN`: `Sample By Fill` = fill cursor runs, `Sample By` = fell through to legacy.

Post-rewrite expectations per RESEARCH.md:
- Defect 2: HIGHLY LIKELY absorbed (toTop() no longer manages `simplePrev[]` flags; uses single `simplePrevRowId = -1L` reset).
- Defect 1: UNCLEAR (may require factory-plumbing fix).

Scope boundary for this plan:
- Restore assertion #1 and assertion #2 to master's 3-row form.
- Investigate Defect 1 post-rewrite; fix in-phase if required per D-06.
- Do NOT touch assertion #3 here — Plan 06 handles it alongside `testFillInsufficientFillValues`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore testSampleByFillNeedFix assertion #1 to master's 3-row form (verify Defect 2 absorbed by rowId rewrite)</name>
  <files>core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java</files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java lines 5943-6100 (current testSampleByFillNeedFix)
    - Master's version via `git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java` — locate testSampleByFillNeedFix and copy the exact expected rows strings
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md §5.1, §5.2 (current vs master form; absorption analysis)
    - .planning/seeds/SEED-002-phase12-cursor-defects-rowid-dependent.md (Defect 2 reproduction narrative)
  </read_first>
  <behavior>
    - Test 1: `testSampleByFillNeedFix` assertion #1 passes against the rowId-rewritten cursor — 3 rows produced, not 6, `cnt=0` for the gap hour, `high=0.00128` for the gap row.
    - Test 2: `sizeExpected=true` double-iteration produces identical rows on second iteration (Defect 2 absorbed — `toTop()` fully resets state including `simplePrevRowId = -1L` and `hasSimplePrev = false`).
    - Test 3: Plan output for the query contains `Sample By Fill` (fast path).
  </behavior>
  <action>
    Step 1 — Extract master's exact expected string for assertion #1:
    ```bash
    git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java > /tmp/master-SampleByTest.java
    grep -n "testSampleByFillNeedFix\|@Test" /tmp/master-SampleByTest.java | grep -B1 "testSampleByFillNeedFix"
    ```
    Locate the method and read its body. Copy the exact three-row expected multiline string AND the query text AND the `assertQuery(...)` call signature.

    Step 2 — In `core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java`, locate `testSampleByFillNeedFix` (approximately line 5943). Replace assertion #1's expected string and `assertSql`/`assertQuery` call with master's form. Preserve:
    - `@Test` annotation.
    - `assertMemoryLeak(() -> { ... })` wrapping.
    - Any `execute(...)` DDL setup lines that create the underlying table.
    - The `timestamp` argument and `sizeExpected=true` flag used by master (these are what force the double-iteration that exposes Defect 2).

    Step 3 — Leave assertion #2 and assertion #3 UNCHANGED in this task. Task 2 handles assertion #2; Plan 06 handles assertion #3.

    Step 4 — Run the test:
    ```bash
    mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2>&1 | tail -25
    ```
    Expected: `BUILD SUCCESS`. If assertion #1 passes, Defect 2 is confirmed absorbed.

    If assertion #1 fails with "expected 3 rows, got N > 3" or similar state-leak signal:
    - Capture the exact diff between expected and actual.
    - Verify Plan 02's `toTop()` reset is complete — re-read `SampleByFillCursor.toTop()` and confirm `simplePrevRowId = -1L; hasSimplePrev = false;` are present along with `keysMap.clear()`.
    - If missing, patch `SampleByFillRecordCursorFactory.java` to add the missing reset as part of this task (it is a Plan-02 leak that Plan-05 catches).
    - If present but test still fails, Defect 2 is NOT fully absorbed. Escalate — Task 2 already has the `autonomous: false` handling; repurpose it.

    If assertion #1 passes but the plan text does not contain `Sample By Fill` (unlikely after retro-fallback deletion in Plan 04 but verify):
    ```bash
    mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix -Dsurefire.printSummary=true -Dsurefire.useFile=false test 2>&1 | grep "Sample By"
    ```
    If the plan shows `Sample By` (legacy), Defect 1 shadow has hit — escalate to Task 2.

    Step 5 — Do NOT commit yet. Task 2 combines assertion-#2 restoration and any Defect-1 fix into a single commit.
  </action>
  <verify>
    <automated>mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2&gt;&amp;1 | tail -10 | grep -q 'BUILD SUCCESS'</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2>&1 | tail -15` contains `BUILD SUCCESS`. (If it reports a failure on assertion #2, that's expected at this task boundary — Task 2 will address. For this task's acceptance, at MINIMUM assertion #1 must pass before assertion #2's failure point.)
    - `grep -A5 "testSampleByFillNeedFix" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java | head -80 | grep -c "assertQuery"` returns >= 1 (assertion #1 uses assertQuery with sizeExpected=true form).
    - The assertion #1 expected-rows string matches master exactly: run a diff comparing the extracted master method body to the current state — zero substantive differences in the expected string, query string, or assertQuery call args for assertion #1.
    - `grep -c "cnt=0\|0.00128\|cnt = 0" core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java` returns at least 1 new match within `testSampleByFillNeedFix` (the gap-row value that master expected but the workaround removed).
    - If Task 1 had to patch `SampleByFillRecordCursorFactory.toTop()` for a missing reset, the patch is minimal: `git diff HEAD -- core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java` shows only reset-line additions (e.g., `simplePrevRowId = -1L;`, `hasSimplePrev = false;`).
  </acceptance_criteria>
  <done>
    Assertion #1 of `testSampleByFillNeedFix` matches master's 3-row form and passes against the rowId-rewritten cursor. Defect 2 confirmed absorbed. No commit yet — Task 2 finalizes.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Restore assertion #2 to master's 3-row assertQuery form; investigate Defect 1 disposition; fix in-phase if required</name>
  <files>core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java, core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java (conditional), core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java (conditional)</files>
  <read_first>
    - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java lines 6022-6052 (current assertion #2 6-row workaround)
    - Master's testSampleByFillNeedFix assertion #2 via `git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java`
    - .planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-RESEARCH.md §5.3, §7.1 (Defect 1 absorption hypothesis, risk section)
    - .planning/seeds/SEED-002-phase12-cursor-defects-rowid-dependent.md (Defect 1 reproduction + hypothesis)
    - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java around `generateFill` — specifically the `timestampIndex < 0` guard at approximately line 3361 (locate via grep after Plan 02/04 edits)
  </read_first>
  <behavior>
    - Terminal behavior after Task 2 completes: assertion #2 of `testSampleByFillNeedFix` passes against master's 3-row assertQuery form. Plan output for the CTE+outer-projection query contains `Sample By Fill`.
    - Task 2 is decision-gated because the fix surface depends on the investigation outcome: no fix needed (absorbed), small factory-plumbing fix, or escalation to user.
  </behavior>
  <action>
    Step 1 — Extract master's exact assertion #2 block:
    ```bash
    git show master:core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java | awk '/public void testSampleByFillNeedFix/,/^    }$/' > /tmp/master-testSampleByFillNeedFix.java
    ```
    Identify the assertion-#2 block (CTE + outer projection form). Copy its expected string, query, and `assertQuery(...)` signature exactly.

    Step 2 — In our branch's `SampleByTest.java`, replace the current assertion #2 (6-row `assertSql` form) with master's 3-row `assertQuery` form. Preserve `assertMemoryLeak(() -> { ... })` wrapping and any shared setup.

    Step 3 — Run the test BEFORE touching any production code:
    ```bash
    mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2>&1 | tail -30
    ```

    Step 4 — Disposition branching:

    **Branch A: assertion #2 PASSES (Defect 1 absorbed by rowId rewrite).**
    - No production code change needed.
    - Commit. This is Commit 5 of phase 13 per D-07. Commit title: e.g. `Restore testSampleByFillNeedFix to master's 3-row form`. Body documents absorption of Defects 1 and 2 by the rowId rewrite; references SEED-002 closure; references phase 12 Option A deferral closed.
    - Jump to Task 2 done.

    **Branch B: assertion #2 FAILS with wrong values but plan shows `Sample By Fill` (fill cursor runs, FillRecord column index mapping bug).**
    - Root cause is in `FillRecord`'s column index mapping under outer-projection remap.
    - Diagnostic: run the failing query with `EXPLAIN` via `assertPlanNoLeakCheck` to confirm plan text. Then isolate: does `SELECT * FROM sq` from the CTE work correctly? If yes, the outer-projection column remap is corrupting the index passed to `FillRecord.getXxx(col)`.
    - Fix location candidates (investigate in order):
      1. `SampleByFillRecordCursorFactory` constructor — how does it build `outputColToKeyPos`, `fillModes`, and any column-index mapping passed to `FillRecord`?
      2. `SqlCodeGenerator.generateFill()` — does it pass the OUTER projection's metadata to the factory, or the inner GROUP BY metadata? This determines whether `FillRecord` column indices are remapped correctly for the outer projection.
    - Apply the minimal surgical fix at the layer where the bug lives.
    - Re-run the test. Must pass.

    **Branch C: assertion #2 FAILS with wrong timestamps (`1970-01-01T00:00:00`) or plan shows `Sample By` (legacy bypass).**
    - Root cause is the `timestampIndex < 0` guard in `generateFill()` at approximately line 3361 (post-Plan-04 line number; locate via `grep -n "timestampIndex" core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java`).
    - Outer CTE projection exposes different metadata to `generateFill` — the timestamp index in the outer projection may differ from the inner GROUP BY's timestamp index.
    - Fix location candidates (investigate in order):
      1. `generateFill()` resolves `timestampIndex` against the wrong metadata object. Fix: thread the inner GROUP BY metadata's timestamp index through the outer projection.
      2. The outer projection drops the timestamp column index without updating the fill path.
    - The fix scope may be non-trivial (per RESEARCH.md §7.1 this could be a 1-2 day investigation). Per `autonomous: false` gating:
      - Pause and surface to the user:
        - Minimal reproduction (extracted CTE query + expected plan vs actual).
        - Proposed fix locus (`generateFill`'s timestampIndex lookup OR outer-projection plumbing).
        - Estimated size of fix (trivial / small / medium / large).
      - User decides: fix in-phase (D-06 commitment) or escalate to a follow-up phase (overrides D-06).
    - After user response, apply the agreed fix. Re-run test. Must pass.

    **Branch D: assertion #2 passes but assertion #1 regresses or other tests regress.**
    - Unexpected. Capture diagnostics and escalate to user.

    Step 5 — Once assertion #2 passes (via Branch A, B, or C), run the full phase test surface:
    ```bash
    mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test 2>&1 | tail -30
    ```
    Must report `BUILD SUCCESS` with zero failures and zero errors.

    Step 6 — Commit Task 1 + Task 2 edits (test + any production-code fix) as Commit 5 of phase 13 per D-07. Commit title (short, plain English): e.g. `Restore testSampleByFillNeedFix to master's 3-row form` (Branch A) OR `Restore testSampleByFillNeedFix and fix CTE+outer-projection fill index mapping` (Branches B/C). Body states:
    - Which SEED-002 defects were resolved (2 always; 1 either absorbed or fixed with pointer to the production change).
    - References: CONTEXT.md D-06, Phase 12 Success Criterion #1 deferred via Option A.
    - Combined-suite ordering confirmation.
  </action>
  <what-built>
    - Task 2 restored assertion #2 of `testSampleByFillNeedFix` to master's 3-row `assertQuery` form.
    - If Branch A fired: no production-code change; Defect 1 absorbed.
    - If Branch B fired: factory-plumbing fix to FillRecord column index mapping.
    - If Branch C fired: generateFill timestampIndex resolution fix, potentially after user decision on scope.
  </what-built>
  <how-to-verify>
    1. Confirm `mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test` exits 0 (all three assertions pass — including assertion #3 if Plan 06 ran, otherwise assertion #3 remains in its workaround state until Plan 06).
    2. Confirm `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test` exits 0.
    3. Confirm the plan for the assertion-#2 query contains `Sample By Fill`.
    4. If Branch C fired, the user confirmed the fix scope before Task 2 proceeded.
  </how-to-verify>
  <verify>
    <automated>mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2&gt;&amp;1 | tail -10 | grep -q 'BUILD SUCCESS'</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test 2>&1 | tail -15` contains `BUILD SUCCESS` and zero failures (ALL assertions except #3 pass; assertion #3 may still be in workaround state until Plan 06).
    - Grep of the test body shows assertion #2 uses `assertQuery` (NOT `assertSql`): within the method body, `grep -c "assertSql" <section>` on the assertion-#2 block returns 0, and `grep -c "assertQuery" <section>` returns >= 2 (both assertion #1 and assertion #2).
    - `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test 2>&1 | tail -30` contains `BUILD SUCCESS` and zero failures in the final summary.
    - If Branch B or C fired, the production-code diff is minimal and documented in the commit body; git diff for either `SqlCodeGenerator.java` or `SampleByFillRecordCursorFactory.java` shows targeted changes only (no drive-by refactors).
    - The phase 12 Option A deferral for `testSampleByFillNeedFix` is closed — SEED-002 Defect 1 and Defect 2 are fully resolved per D-06.
    - `git log -1 --pretty=%s` matches a plain-English short title.
  </acceptance_criteria>
  <resume-signal>Type "approved — branch A applied" (absorbed, no code change) or "approved — branch B/C applied, fix at {location}" or "escalate — deferring Defect 1 to follow-up phase" (overrides D-06).</resume-signal>
  <done>
    Assertion #2 restored to master's 3-row `assertQuery` form; all three testSampleByFillNeedFix assertions' statuses match master (assertion #3 still in workaround if Plan 06 has not run yet). Full phase suite green. SEED-002 Defects 1 and 2 resolved per D-06 OR user-approved deferral recorded. Commit 5 of phase 13 in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal refactor — no new auth or input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | `toTop()` state leak hiding under sizeExpected=true double-iteration | mitigate | Task 1 restores assertion #1 (`sizeExpected=true` form); rerun verifies all state (simplePrevRowId, hasSimplePrev, isGapFilling, etc.) resets correctly. |
| T-13-05-02 | Tampering | CTE + outer-projection produces wrong timestamps (Defect 1) | mitigate | Task 2's branching logic investigates post-rewrite; fixes at the correct layer (fill index mapping OR generateFill timestampIndex resolution) OR escalates to user per D-06. Plan output verified to contain `Sample By Fill`. |
| T-13-05-03 | Denial of Service | Fix scope explosion (Defect 1 root cause deeper than expected) | mitigate | Task 2 is `autonomous: false`; user approves scope before non-trivial fix lands, and D-06 deferral is permitted as a documented override. |
</threat_model>

<verification>
- `mvn -pl core -Dtest=SampleByTest#testSampleByFillNeedFix test` exits 0.
- `mvn -pl core -Dtest='SampleByFillTest,SampleByTest,SampleByNanoTimestampTest,ExplainPlanTest,RecordCursorMemoryUsageTest' test` exits 0.
- `testSampleByFillNeedFix` assertion #1 uses master's `assertQuery` with `sizeExpected=true`.
- `testSampleByFillNeedFix` assertion #2 uses master's `assertQuery` (3-row form).
- SEED-002 Defect 1 and Defect 2 resolved or user-approved deferral recorded.
</verification>

<success_criteria>
- Commit 5 of phase 13 landed.
- `testSampleByFillNeedFix` matches master's 3-row form in both assertion #1 and #2.
- Defects 1 and 2 from SEED-002 fully resolved per D-06 (or user-approved deferral).
- Full phase test surface green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-fill-prev-snapshots-from-materialized-values-to-rowi/13-05-SUMMARY.md` documenting:
- Which disposition branch fired (A/B/C).
- For Branch A: confirmation of absorption for both defects.
- For Branch B/C: the exact production-code change and rationale.
- `testSampleByFillNeedFix` before/after (assertion counts, assertion types).
- SEED-002 closure status.
- Confirmation that Plan 06 is unblocked.
</output>
