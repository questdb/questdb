---
phase: 11-hardening-review-findings-fixes-and-missing-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
retroactive: true
files_modified:
  - core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
  - core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
  - core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByNanoTimestampTest.java
autonomous: true
requirements:
  - CR-UUID-FILL-KEY
  - CR-GEO-NULL-SENTINEL
  - CR-DECIMAL-NULL-SENTINEL
  - CR-NULL-KEY-TEST
  - CR-CTE-RECLASSIFICATION-TEST
  - CR-DST-SPARSE-TEST

must_haves:
  truths:
    - "FillRecord getLong128Hi/Lo, getDecimal128/256, getLong256/A/B dispatch FILL_KEY to keysMapRecord so UUID and Long256 key columns emit their actual values, not null"
    - "FillRecord getGeoByte/Short/Int/Long null sentinels use GeoHashes.BYTE_NULL/SHORT_NULL/INT_NULL/NULL (not 0 or Numbers.*_NULL)"
    - "FillRecord getDecimal8/16/32/64 null sentinels use Decimals.DECIMAL*_NULL"
    - "SampleByFillTest.testFillNullKeyedWithNullKey proves NULL SYMBOL key forms its own fill group"
    - "SampleByFillTest.testFillPrevKeyedCte proves FILL_KEY reclassification inside a CTE/subquery works"
    - "SampleByFillTest.testFillNullDstSparseData proves sparse data around DST fall-back generates fill rows during the transition"
    - "/review-pr produces no critical or moderate findings on production code"
  artifacts:
    - path: "core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java"
      provides: "FILL_KEY dispatch for UUID/Long256/Decimal128-256 getters; GeoHashes.*_NULL and Decimals.*_NULL sentinels"
      contains: "GeoHashes.BYTE_NULL"
    - path: "core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java"
      provides: "testFillNullKeyedWithNullKey, testFillPrevKeyedCte, testFillNullDstSparseData"
      contains: "testFillPrevKeyedCte"
  key_links:
    - from: "FillRecord.getLong128Hi/Lo (UUID)"
      to: "keysMapRecord"
      via: "FILL_KEY dispatch branch before FILL_PREV / FILL_CONSTANT"
      pattern: "fillMode.*FILL_KEY"
    - from: "FillRecord.getGeo*"
      to: "GeoHashes.*_NULL"
      via: "return statement for null sentinel (replaces 0 / Numbers.*_NULL)"
      pattern: "GeoHashes.*_NULL"
---

<objective>
Close out code-review findings from the PR #6946 review cycle: six hardening items scoped in `11-CONTEXT.md` — three code fixes (UUID FILL_KEY dispatch, geo null sentinels, decimal null sentinels) and three missing tests (NULL SYMBOL key, CTE/subquery FILL_KEY reclassification, sparse DST).

Purpose: `11-CONTEXT.md` captures the six items. Each one surfaced from `/review-pr` against the fast-path fill implementation. The fixes are local and mechanical; verification is a re-run of `/review-pr`.

Output: Clean review with no critical/moderate findings and three new regression tests guarding the hardened paths.

**Note:** This plan is retroactive. The work described below already landed in commits `2125201f30`, `28b00e8340`, and `a6355c3e65`. The plan exists to reconcile GSD metadata with what was executed directly from review-pr findings while those findings were in-context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-review-findings-fixes-and-missing-test-coverage/11-CONTEXT.md
@core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
@core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: FILL_KEY dispatch for 128/256-bit key types and geo/decimal null sentinels</name>
  <files>
    core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByFillTest.java
  </files>
  <action>
  Inside the `FillRecord` inner class (SampleByFillRecordCursorFactory), extend the FILL_KEY dispatch pattern — already used by getInt/getLong/getByte/getShort/getSymbol — to the remaining 128/256-bit key-carrying getters, and replace incorrect null sentinels.

  Getters that gain FILL_KEY dispatch:
  - `getLong128Hi(int col)` and `getLong128Lo(int col)` — required for UUID key columns
  - `getDecimal128(int col, Long128Sink sink)` and `getDecimal256(int col, Long256Sink sink)`
  - `getLong256(int col)`, `getLong256A(int col)`, `getLong256B(int col)`

  Dispatch shape (mirrors existing getInt/getLong pattern):
  ```java
  int mode = fillMode(col);
  if (mode == FILL_KEY) {
      return keysMapRecord.getLong128Hi(col);  // or equivalent accessor
  }
  if (mode == FILL_PREV_SELF || mode >= 0) {
      if (hasKeyPrev()) return prevValue(col);  // for scalar getters
  }
  // existing FILL_CONSTANT / default branch
  ```

  Null sentinels:
  - `getGeoByte`:  `0` → `GeoHashes.BYTE_NULL`
  - `getGeoShort`: `0` → `GeoHashes.SHORT_NULL`
  - `getGeoInt`:   `Numbers.INT_NULL` → `GeoHashes.INT_NULL`
  - `getGeoLong`:  `Numbers.LONG_NULL` → `GeoHashes.NULL`
  - `getDecimal8`:  `0` → `Decimals.DECIMAL8_NULL`
  - `getDecimal16`: `0` → `Decimals.DECIMAL16_NULL`
  - `getDecimal32`: `Numbers.INT_NULL`  → `Decimals.DECIMAL32_NULL`
  - `getDecimal64`: `Numbers.LONG_NULL` → `Decimals.DECIMAL64_NULL`

  Imports: add `io.questdb.cairo.GeoHashes` and `io.questdb.std.Decimals`.

  Three new regression tests in SampleByFillTest.java. Follow existing conventions (assertMemoryLeak / execute / assertQueryNoLeakCheck with "ts" timestamp column).

  - `testFillNullKeyedWithNullKey` — SYMBOL key column with a NULL value must form its own fill group (cartesian product includes a NULL row per bucket).
  - `testFillPrevKeyedCte` — `WITH q AS (SELECT ... SAMPLE BY 1h FILL(PREV) ...) SELECT * FROM q`; the CTE wrapper must keep SYMBOL key column classified as FILL_KEY, not silently reclassified as an aggregate.
  - `testFillNullDstSparseData` — Europe/Riga DST fall-back with data only before and after the transition; fill rows must be emitted during the duplicated hour without duplication or gaps.
  </action>
  <verify>
    <automated>cd /Users/sminaev/qdbwt/elegant-cuddling-sprout && mvn -pl core -Dtest=SampleByFillTest -DfailIfNoTests=false test 2>&1 | tail -20</automated>
  </verify>
  <done>
  - UUID and Long256 key columns emit correct values in fill rows
  - Geo and decimal null sentinels use the right constants
  - Three new tests pass; pre-existing SampleByFillTest still green
  </done>
</task>

<task type="auto">
  <name>Task 2: Ownership transfer, dead code removal, safety-net documentation</name>
  <files>
    core/src/main/java/io/questdb/griffin/SqlCodeGenerator.java
  </files>
  <action>
  Three orthogonal hygiene fixes uncovered by the same review cycle:

  1. Transfer `Function` ownership from `fillValues` to `constantFillFuncs` by nulling the source slot after the move — prevents double-`close()` on the error path.
     ```java
     constantFillFuncs.add(fillValues.getQuick(fillIdx));
     fillValues.setQuick(fillIdx, null);  // transfer ownership
     ```

  2. Delete `guardAgainstFillWithKeyedGroupBy` — phase 3 removed its last caller. The 21-line method is dead.

  3. Expand the safety-net comment that reclassifies SYMBOL columns as keys late in codegen. Explain *why* the reclassification is correct: the optimizer gate already blocks genuine aggregates with unsupported PREV types, so the safety net only fires for misidentified key columns in CTE/subquery contexts. Without the comment, a future reader is tempted to remove the net as unreachable.
  </action>
  <done>
  - No double-close on fill-generation error path
  - Dead method removed
  - Safety-net intent captured in-code for the next reader
  </done>
</task>

<task type="auto">
  <name>Task 3: Minor naming, sentinel, and test-expectation fixes</name>
  <files>
    core/src/main/java/io/questdb/griffin/engine/groupby/SampleByFillRecordCursorFactory.java
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByTest.java
    core/src/test/java/io/questdb/test/griffin/engine/groupby/SampleByNanoTimestampTest.java
  </files>
  <action>
  Production:
  - Rename `baseCursorExhausted` → `isBaseCursorExhausted` (CLAUDE.md boolean naming convention).
  - Use `Decimals.DECIMAL32_NULL` / `Decimals.DECIMAL64_NULL` in getDecimal32/64 (completes Task 1's sentinel work for the 32/64-bit variants).
  - Reorder fields in `SampleByFillCursor` alphabetically within visibility group.

  Tests (value-only; `assertQuery` call structure preserved):
  - Geo PREV null rendering: `"000"` → `""` (now that `GeoHashes.*_NULL` renders as empty string).
  - Long256 key fill-row rendering: `""` → correct hex values (now that FILL_KEY dispatches to keysMapRecord).

  Mirror the same two test-value updates in SampleByNanoTimestampTest to keep nano/micro paths in lockstep.
  </action>
  <verify>
    <automated>cd /Users/sminaev/qdbwt/elegant-cuddling-sprout && mvn -pl core -Dtest=SampleByTest,SampleByNanoTimestampTest,SampleByFillTest -DfailIfNoTests=false test 2>&1 | tail -30</automated>
  </verify>
  <done>
  - Boolean field follows naming convention
  - All decimal getters use Decimals.*_NULL
  - Test expectations reflect correct geo/Long256 fill-row values
  - SampleByTest + SampleByNanoTimestampTest + SampleByFillTest all green
  </done>
</task>

<task type="manual">
  <name>Task 4: Re-run /review-pr</name>
  <action>
  Run `/review-pr 6946 from scratch, ignoring planning folder` against the PR head after Tasks 1–3 land. Verify no critical/moderate findings remain on production code.
  </action>
  <done>
  - /review-pr produces no critical/moderate findings
  - Any new minor findings are captured as follow-up or dismissed with rationale
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are internal to the fill cursor and its tests.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | I (Information Disclosure) | UUID/Long256 FILL_KEY dispatch | mitigate | Without Task 1, UUID key columns silently emit null in fill rows — consumers see gaps where a key existed. FILL_KEY dispatch guarantees the actual key value reaches the caller. |
| T-11-02 | I (Information Disclosure) | Geo/decimal null sentinels | mitigate | Wrong sentinel (0 vs NULL constant) makes a null value indistinguishable from a legitimate zero. Using `GeoHashes.*_NULL` / `Decimals.*_NULL` preserves the is-null signal to downstream code. |
| T-11-03 | D (Denial of Service) | Error-path double-close | mitigate | Task 2 nulls `fillValues` after ownership transfer so exception-path cleanup does not double-free a native Function. |
</threat_model>

<verification>
```bash
cd /Users/sminaev/qdbwt/elegant-cuddling-sprout && \
  mvn -pl core -Dtest=SampleByFillTest,SampleByTest,SampleByNanoTimestampTest -DfailIfNoTests=false test
```
</verification>

<success_criteria>
1. `testFillNullKeyedWithNullKey`, `testFillPrevKeyedCte`, `testFillNullDstSparseData` all pass
2. All pre-existing SampleByFillTest + SampleByTest + SampleByNanoTimestampTest still pass
3. `/review-pr` reports no critical or moderate findings on production code
4. PR #6946 CI remains green
</success_criteria>

<output>
After completion, create `.planning/phases/11-hardening-review-findings-fixes-and-missing-test-coverage/11-01-SUMMARY.md`.
</output>
