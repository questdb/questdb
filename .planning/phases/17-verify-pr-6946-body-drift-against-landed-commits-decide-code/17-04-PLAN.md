---
phase: 17-verify-pr-6946-body-drift-against-landed-commits-decide-code
plan: 04
type: execute
wave: 3
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - .planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md
  - .planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md
autonomous: true
requirements: [COR-04, XPREV-01]
addresses_decisions: [D-03, D-05, D-06, D-11, D-24, D-28]
deliberate_noops:
  - id: D-02
    rationale: "M3.1 `9df205bac5` TIMESTAMP unit drift + non-TIMESTAMP alias guard — pure mid-term regression recovery per CONTEXT.md D-02; no PR body row, no new test. D-01 principle applies."
  - id: D-04
    rationale: "M3.3 `82865efbc0` Phase 16 FUNCTION/OPERATION classifier — closed by Phase 16 Plan 01 per CONTEXT.md D-04; no Phase 17 work. D-01 principle applies."

must_haves:
  truths:
    - "PR #6946 body contains the new M1 pre-1970 FILL guard row in 'What's covered' block 2, verbatim per plan file."
    - "PR #6946 body's K x B circuit-breaker Future-work bullet disambiguates cancellation CB (landed) from memory-budget CB (still future), verbatim per plan file."
    - "PR #6946 body's two stale M3.4 sentences (Implementation-notes pitfall + 'ALIGN TO CALENDAR WITH OFFSET + FILL without TO' row) are rewritten per plan-file verbatim wording. No optional 'sub-day + TZ + FROM + OFFSET' row added (per RESEARCH.md D-05 master-support verdict)."
    - "PR #6946 body's Test plan no longer contains the two test-count lines ('SampleByFillTest: 110 tests' and 'SampleByNanoTimestampTest: 278 tests')."
    - "PR #6946 title is 'feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path'."
    - "The pending todo file for D-26 is moved to `.planning/todos/completed/` with `completed_at` and `completed_in` fields."
  artifacts:
    - path: "GitHub PR #6946 body (external, via gh)"
      provides: "body with M1 row, rewritten K x B bullet, rewritten M3.4 sentences, no test-count lines"
      contains: "SAMPLE BY FILL on a table with pre-1970 timestamps"
    - path: "GitHub PR #6946 title (external, via gh)"
      provides: "title renamed to 'feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path'"
      contains: "cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path"
    - path: ".planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md"
      provides: "Retired todo file with completed_at + completed_in back-reference to 17-01"
      contains: "completed_in: 17-01-PLAN.md"
  key_links:
    - from: "PR body M1 row"
      to: "sqllogictest pre-1970 FILL coverage (already on branch)"
      via: "row placement in 'What's covered' block 2 near the other fast-path corner-case rows"
      pattern: "pre-1970 timestamps"
    - from: "PR body K x B bullet"
      to: "Plans 01 pass-1 CB fix + Phase 15 Plan 02 emission CB fix"
      via: "parenthetical 'Unrelated to the cancellation SqlExecutionCircuitBreaker, which is honoured by the fill cursor.'"
      pattern: "Unrelated to the cancellation"
    - from: "PR body M3.4 rewritten sentences"
      to: "commit 289d43090a widened setFillOffset condition"
      via: "sentences correctly describe the `sampleByTimezoneName == null || (hasSubDayTimezoneWrap && sampleByFrom != null)` predicate"
      pattern: "hasSubDayTimezoneWrap"
    - from: "Completed todo file"
      to: "Plan 01 Task 2 commit (D-26 fix)"
      via: "completed_in: 17-01-PLAN.md"
      pattern: "completed_in: 17-01-PLAN.md"
---

<objective>
Ship the final cluster of PR metadata edits for Phase 17:

1. **D-11 (M1 pre-1970 row):** add the plan-file-verbatim row to "What's covered" block 2 — net-new behavior vs master (cursor path rejected pre-1970; fast path handles negative timestamps natively via `timestamp_floor_utc`).
2. **D-03 (M3.2 K x B bullet rewrite):** append the plan-file-verbatim parenthetical disambiguating cancellation CB (landed in `c1deb9b14d` + Plan 01 pass-1) from the memory-budget K x B CB (still Future work).
3. **D-05 (M3.4 stale sentences):** rewrite the two currently-false sentences (Implementation-notes pitfall + "ALIGN TO CALENDAR WITH OFFSET + FILL without TO" row) per plan-file verbatim wording. SKIP the optional "sub-day + TZ + FROM + OFFSET" row per RESEARCH.md D-05 master-support verdict.
4. **D-06 (M3.5 count lines removal):** delete the two test-count lines from the Test plan section.
5. **D-24 (m9 title rename):** `gh pr edit 6946 --title "feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path"`.
6. **D-28 (todo retirement):** move `.planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md` to `.planning/todos/completed/` with `completed_at` + `completed_in: 17-01-PLAN.md` back-references.

Purpose: land PR metadata in a single clean commit after Plans 01-03 have added their regression tests (per D-25 ordering rule: metadata lands last). The D-01 principle governs — regression recovery (M3.1, M3.2 cancellation-CB, M3.3, M-new, M-unit) does NOT get body rows; only net-new behavior (M1) and corrections of currently-false text (M3.2 bullet, M3.4 sentences) land.

**ASCII-only rule (applied phase-wide to PR body / commit / comment text):** every plan-file-verbatim edit in this plan ships content-verbatim with em-dash rendered as `--` (plain ASCII hyphen-minus pair). The plan file itself uses Unicode em-dash `—` and Unicode multiplication `×`; this plan substitutes both with ASCII (`--` and `x` respectively) on the way into the PR body. Acceptance checks include a non-ASCII byte count on the updated body.

**SC#4 scope collapse vs ROADMAP:** ROADMAP SC#4 originally listed four new "What's covered" rows (M3.1 x 2 + M3.2 + M3.4 + M1). RESEARCH.md's D-05 master-support verdict + CONTEXT.md D-01 regression-recovery principle collapse this to a single row (M1 only). The three omitted rows are explicit no-op documentation: M3.1 rows omitted per D-02, M3.2 row omitted per D-03 (only the K x B bullet gets rewritten, not a new row), M3.4 row omitted per D-05 master-support verdict. Plan 04 delivers 1 row + 1 bullet rewrite + 2 sentence rewrites + 2 line deletions instead of 4 rows, and this is the intended state.

Output:
- 1 `gh pr edit 6946 --body` call with the updated body.
- 1 `gh pr edit 6946 --title` call with the new title.
- 1 `git mv` (or equivalent) moving the pending todo file to `completed/`.
- 1 git commit for the todo move (the `gh pr edit` calls don't produce commits — PR body + title live on GitHub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-verify-pr-6946-body-drift-against-landed-commits-decide-code/17-CONTEXT.md
@.planning/phases/17-verify-pr-6946-body-drift-against-landed-commits-decide-code/17-RESEARCH.md
@.planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md

<interfaces>
<!-- Verbatim PR-body edit wording copied from the plan file, locked per D-03/D-05/D-11/D-24. -->

**D-11 M1 row (plan-file verbatim, "What's covered" block 2):**

| Query shape | Master | This PR |
|---|---|---|
| SAMPLE BY FILL on a table with pre-1970 timestamps | Cursor path rejected with `cannot FILL for the timestamps before 1970` | Works correctly -- `timestamp_floor_utc` handles negative timestamps natively |

(ASCII only. Replace plan-file em-dash with `--` when rendering into the Markdown body since `gh pr edit` passes the body through to GitHub and GitHub Markdown renders `--` as a dash.)

**D-03 K x B bullet — FULL rewrite (plan-file verbatim):**

> **`K x B` memory circuit-breaker.** A size-estimated short-circuit in `generateFill` that falls back to the cursor path when `unique_keys x buckets` exceeds a configurable threshold, capping the `SortedRecordCursor` materialisation footprint. (Unrelated to the cancellation `SqlExecutionCircuitBreaker`, which is honoured by the fill cursor.)

This REPLACES whatever the current K x B bullet says in the "Easy extensions" / Future work section (research confirmed current body uses `x` (multiplication glyph) in the current wording; pick the matching glyph used by the current body — likely Unicode `x` but replace with ASCII `x` if any other non-ASCII is present).

**D-05 two stale sentence rewrites (plan-file verbatim):**

*Sentence 1 — Implementation-notes "Calendar offset alignment" pitfall.* Current (stale):

> *Pitfall:* offset must NOT be double-applied when `timestamp_floor_utc` already accounts for it (timezone case). The optimizer only sets `fillOffset` when `sampleByTimezoneName == null`.

Replacement (plan-file verbatim):

> *Pitfall:* offset must NOT be double-applied when `timestamp_floor_utc` already accounts for it. The optimizer sets `fillOffset` when `sampleByTimezoneName == null`, or -- under sub-day + TZ + FROM -- when `rewriteSampleBy` has already stripped TZ from `timestamp_floor_utc` and wrapped FROM in `to_utc(FROM, tz)`, making the GROUP BY grid purely UTC-anchored and requiring the fill cursor to reproduce the `+ offset` shift.

*Sentence 2 — "ALIGN TO CALENDAR WITH OFFSET + FILL without TO" row.* Current (stale) third column contains:

> Fixed via `QueryModel.fillOffset` propagation; offset applied only when `sampleByTimezoneName == null`...

Replacement (plan-file verbatim):

> Fixed via `QueryModel.fillOffset` propagation; offset applied when `sampleByTimezoneName == null`, and also when sub-day + TZ + FROM requires it (the timezone case is otherwise absorbed by `timestamp_floor_utc`).

**D-06 — REMOVE (not rewrite) the two test-count lines.** Per RESEARCH.md these read as:
- `SampleByFillTest: 110 tests` (roughly — the current body uses this shape)
- `SampleByNanoTimestampTest: 278 tests mirror the microsecond suite`

Delete both lines entirely from the Test plan section. Leave the surrounding Test plan structure intact.

**D-24 title:**

Exact new title: `feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path`

Current (stale) title: `feat(sql): add FILL(PREV) support on GROUP BY fast path`.

**D-28 todo retirement — target file content:**

`.planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md` must match the source file's content with two front-matter additions at the top of the YAML block:
```yaml
completed_at: 2026-04-22
completed_in: 17-01-PLAN.md
```

(Date reflects when Plan 01 lands; adjust to actual Plan 01 merge/commit date if different.)

**gh CLI anchors:**
- `gh pr view 6946 --json body --jq '.body' > /tmp/pr6946-body.md` — fetches current body for local editing.
- `gh pr edit 6946 --body-file /tmp/pr6946-body.md` — updates body.
- `gh pr edit 6946 --title "..."` — updates title.
- `gh pr view 6946 --json body --jq '.body'` — re-fetch and confirm.

**Research correction to CONTEXT.md D-05 verdict:** CONTEXT.md offered an optional "sub-day + TZ + FROM + OFFSET" row contingent on master-support verdict. RESEARCH.md D-05 resolved: master's cursor path DID support this shape via `SampleByFillNullRecordCursorFactory`'s timezone + offset constructor args. Under D-01 rule (a), this is NOT net-new behavior — fast-path participation is an implementation detail. DO NOT add the optional row. Only the two stale sentences get rewritten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PR #6946 body + rename title + retire D-26 todo (single commit for todo move)</name>
  <files>.planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md, .planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md</files>
  <read_first>
    - ~/.claude/plans/let-s-discuss-issues-one-gentle-elephant.md sections `### M1`, `### M3.2`, `### M3.4`, `### M3.5`, `### m9` (verbatim wording for all edits)
    - .planning/phases/17-verify-pr-6946-body-drift-against-landed-commits-decide-code/17-CONTEXT.md (D-03, D-05, D-06, D-11, D-24, D-28)
    - .planning/phases/17-verify-pr-6946-body-drift-against-landed-commits-decide-code/17-RESEARCH.md section D-05 (master-support verdict: SKIP optional row; two sentences only)
    - .planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md (source file for the todo move; read so the completed copy can preserve content + add two new front-matter fields)
    - Current PR body: `gh pr view 6946 --json body --jq '.body'` (MANDATORY — re-verify anchors before editing; RESEARCH.md marks the body artefact confidence as MEDIUM and explicitly requires re-verification before Plan 04)
  </read_first>
  <action>
**Step 1 — Fetch current body + title:**

```bash
gh pr view 6946 --json body --jq '.body' > /tmp/pr6946-body-before.md
gh pr view 6946 --json title --jq '.title' > /tmp/pr6946-title-before.txt
cp /tmp/pr6946-body-before.md /tmp/pr6946-body.md
```

Read `/tmp/pr6946-body-before.md` fully. Verify the following anchors exist (RESEARCH.md flagged these as MEDIUM confidence due to live-artefact drift; if any anchor is missing, STOP and report — the body may have drifted further since research):

- Two "What's covered" tables (block 1 and block 2). Block 2 is the scope-of-this-PR table containing bug-fix rows.
- An "Easy extensions" / Future work section containing a bullet on `K x B` circuit-breaker.
- An Implementation-notes section containing the "Calendar offset alignment" pitfall sentence.
- A row in "What's covered" block 2 for "`ALIGN TO CALENDAR WITH OFFSET` + FILL without `TO`" containing the stale "offset applied only when `sampleByTimezoneName == null`" phrase.
- A Test plan section containing lines similar to `SampleByFillTest: 110` and `SampleByNanoTimestampTest: 278 tests`.

If all anchors confirmed, proceed. If any missing, STOP and report for human re-planning.

**Step 2 — Apply body edits in `/tmp/pr6946-body.md`:**

Apply these edits in any order that does not break anchor references for subsequent edits. Edit the file directly (use text editor / sed / Edit tool — note that Edit tool requires prior Read of the file). Verify every edit with a grep before moving to the next.

Edit A (D-11 M1 row) — Insert the following row into "What's covered" block 2, near the other fast-path corner-case rows (e.g., adjacent to the `ALIGN TO CALENDAR WITH OFFSET + FILL without TO` row or any pre-1970 / timestamp-edge row):

```markdown
| SAMPLE BY FILL on a table with pre-1970 timestamps | Cursor path rejected with `cannot FILL for the timestamps before 1970` | Works correctly -- `timestamp_floor_utc` handles negative timestamps natively |
```

(ASCII dashes. Verify indentation / pipe alignment matches the surrounding table's style.)

Edit B (D-03 K x B bullet) — Replace the entire existing K x B bullet (wherever it lives in Future work / Easy extensions) with the plan-file verbatim version:

```markdown
**`K x B` memory circuit-breaker.** A size-estimated short-circuit in `generateFill` that falls back to the cursor path when `unique_keys x buckets` exceeds a configurable threshold, capping the `SortedRecordCursor` materialisation footprint. (Unrelated to the cancellation `SqlExecutionCircuitBreaker`, which is honoured by the fill cursor.)
```

Always normalize to ASCII `x` (no Unicode multiplication glyph). The plan-file uses ASCII `x` and the phase-wide ASCII-only rule (stated in <objective>) governs over any pre-existing Unicode glyphs in the current body. If the current body contains `K × B` (Unicode U+00D7), this edit also replaces the glyph as a side effect — the full-bullet replacement naturally overwrites it.

After all body edits land, run a non-ASCII byte count on the updated body as a guard: `gh pr view 6946 --json body --jq '.body' | LC_ALL=C grep -P '[^\x00-\x7F]' | wc -l` MUST return `0`. Any hit indicates a stray Unicode glyph either in the replacement text or elsewhere in the body; debug before the commit lands.

Edit C (D-05 Sentence 1) — Replace the stale Implementation-notes "Calendar offset alignment" pitfall with the plan-file verbatim replacement:

```markdown
*Pitfall:* offset must NOT be double-applied when `timestamp_floor_utc` already accounts for it. The optimizer sets `fillOffset` when `sampleByTimezoneName == null`, or -- under sub-day + TZ + FROM -- when `rewriteSampleBy` has already stripped TZ from `timestamp_floor_utc` and wrapped FROM in `to_utc(FROM, tz)`, making the GROUP BY grid purely UTC-anchored and requiring the fill cursor to reproduce the `+ offset` shift.
```

Edit D (D-05 Sentence 2) — In the "ALIGN TO CALENDAR WITH OFFSET + FILL without TO" row's third column, replace:

> Fixed via `QueryModel.fillOffset` propagation; offset applied only when `sampleByTimezoneName == null`...

with:

> Fixed via `QueryModel.fillOffset` propagation; offset applied when `sampleByTimezoneName == null`, and also when sub-day + TZ + FROM requires it (the timezone case is otherwise absorbed by `timestamp_floor_utc`).

Preserve any trailing text in the same cell (the "..." represents continuation); splice the replacement in place of the stale "offset applied only when..." clause.

Edit E (D-06 count lines removal) — Delete the two lines resembling:

```
SampleByFillTest: 110 tests (or similar count phrasing)
SampleByNanoTimestampTest: 278 tests mirror the microsecond suite
```

Delete the WHOLE lines (including any bullet / dash prefix and trailing newline). The surrounding Test plan structure (bullets for new tests, verification commands) stays. Do NOT add replacement lines — per D-06 the counts are dropped entirely.

Per RESEARCH.md D-05, do NOT add the optional "sub-day + TZ + FROM + OFFSET" row. Master's cursor path supported this shape; under D-01 it's not net-new behavior.

**Step 3 — Apply body update via gh:**

```bash
gh pr edit 6946 --body-file /tmp/pr6946-body.md
```

**Step 4 — Apply title update:**

```bash
gh pr edit 6946 --title "feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path"
```

**Step 5 — Verify body + title via re-fetch:**

```bash
gh pr view 6946 --json body --jq '.body' > /tmp/pr6946-body-after.md
gh pr view 6946 --json title --jq '.title' > /tmp/pr6946-title-after.txt
```

Grep for every anchor; each MUST return the expected result (acceptance_criteria below enumerates the grep pattern set).

**Step 6 — D-28 todo retirement:**

Read `.planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md`. Create the destination file `.planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md` with:
- Same body content as the pending file.
- Two new YAML front-matter fields added at the top of the existing front-matter block:
  ```yaml
  completed_at: <ISO date of Plan 01 commit; fetch via git log for the Plan 01 Commit 2 — the cross-column predicate fix>
  completed_in: 17-01-PLAN.md
  ```

Delete the pending file after the completed file is in place. Use `git mv` if possible (`git mv .planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md .planning/todos/completed/` then edit the destination to add the two front-matter fields) — this preserves file history.

**Step 7 — Commit the todo move:**

Commit. Title: "Retire MICRO/NANO unit-mismatch todo" (<= 50 chars). Long-form body: reference Plan 01 Commit 2 SHA for the fix; note that PR body + title are external (GitHub metadata — no commit captures them on the branch); summarize the D-11 / D-03 / D-05 / D-06 / D-24 body/title edits so the commit history documents Phase 17's external changes. No Conventional Commits prefix.
  </action>
  <verify>
    <automated>test "$(gh pr view 6946 --json title --jq '.title')" = "feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path" &amp;&amp; gh pr view 6946 --json body --jq '.body' | grep -c "SAMPLE BY FILL on a table with pre-1970 timestamps" | grep -q "^1$" &amp;&amp; gh pr view 6946 --json body --jq '.body' | grep -c "Unrelated to the cancellation" | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 6946 --json title --jq '.title'` returns exactly `feat(sql): add cross-column FILL(PREV) and move SAMPLE BY FILL onto fast path`.
    - `gh pr view 6946 --json body --jq '.body' | grep -c "SAMPLE BY FILL on a table with pre-1970 timestamps"` returns `1` (M1 row landed).
    - `gh pr view 6946 --json body --jq '.body' | grep -c "Unrelated to the cancellation"` returns `1` (K x B rewrite landed).
    - `gh pr view 6946 --json body --jq '.body' | grep -c "stripped TZ from \`timestamp_floor_utc\`"` returns `1` (M3.4 sentence 1 rewrite landed).
    - `gh pr view 6946 --json body --jq '.body' | grep -c "also when sub-day + TZ + FROM requires it"` returns `1` (M3.4 sentence 2 rewrite landed).
    - `gh pr view 6946 --json body --jq '.body' | grep -c "offset applied only when"` returns `0` (stale wording removed from both locations).
    - `gh pr view 6946 --json body --jq '.body' | grep -cE "SampleByFillTest: [0-9]+ tests?|SampleByNanoTimestampTest: [0-9]+ tests?"` returns `0` (D-06 count lines removed).
    - `.planning/todos/pending/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md` does NOT exist.
    - `.planning/todos/completed/2026-04-22-reject-cross-column-fill-prev-timestamp-unit-mismatch.md` exists and `grep -c "completed_in: 17-01-PLAN.md"` returns `1`.
    - Single git commit for the todo move (PR body + title are external — no git commits capture them).
    - Commit title <= 50 chars; no Conventional Commits prefix.
    - `gh pr view 6946 --json body --jq '.body' | grep -c "Works correctly -- \`timestamp_floor_utc\`"` returns `1` (M1 row third-column ASCII-dash form landed).
    - `gh pr view 6946 --json body --jq '.body' | LC_ALL=C grep -P '[^\x00-\x7F]' | wc -l` returns `0` (phase-wide ASCII-only rule; no Unicode em-dash, multiplication glyph, or other non-ASCII byte survives in the updated body).
  </acceptance_criteria>
  <done>
PR #6946 body contains the M1 row, the rewritten K x B bullet, the two rewritten M3.4 sentences, no test-count lines, no optional M3.4 "sub-day + TZ + FROM + OFFSET" row. PR title is renamed per D-24. Pending todo file moved to completed with `completed_in: 17-01-PLAN.md`. Single commit captures the todo move; the body + title edits are verified via gh re-fetch but produce no commits (external artefacts).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer <-> GitHub API (gh CLI) | Developer authenticates via gh token; PR body + title edits are authenticated write operations. No end-user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-S | Spoofing | gh authentication | accept | gh CLI uses pre-existing developer credentials (`gh auth status`); no new credential surface. |
| T-17-04-T | Tampering | PR body content | accept | Developer-authored body edits only; subject to GitHub audit log. Reviewers can diff body history before merge. |
| T-17-04-R | Repudiation | PR edit audit trail | N/A | GitHub records body/title edits in the PR timeline; no additional logging needed. |
| T-17-04-I | Information Disclosure | — | N/A | No new data exposure (PR body is already public). |
| T-17-04-D | Denial of Service | — | N/A | No production path. |
| T-17-04-E | Elevation of Privilege | — | N/A | No privilege change. |
</threat_model>

<verification>
- `gh pr view 6946 --json title --jq '.title'` matches the new title exactly.
- Every grep-verifiable condition in acceptance_criteria passes.
- One commit on the branch (todo move).
- `/review-pr 6946` re-run (REQUIRED as phase-exit check per ROADMAP SC#8) shows M1 / M2 / M3.2-bullet / M3.4-sentences / M3.5 / m9 as closed; M3.1 / M3.3 / M-new / M-unit as "closed as regression recovery per D-01" or "closed by Plan 01 Task 2" respectively. If any finding surfaces not-closed, Phase 17 has not delivered ROADMAP Success Criterion #8 and must be re-opened.
</verification>

<success_criteria>
- PR body and title updated; every plan-file-verbatim edit present; stale wording absent.
- D-28 todo retired with completed_at + completed_in back-reference to Plan 01.
- Single commit captures the todo move; external PR metadata edits verified via gh re-fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/17-verify-pr-6946-body-drift-against-landed-commits-decide-code/17-04-SUMMARY.md` documenting:
- The commit SHA (todo move).
- Snapshot of the before/after body diff (or at least: which sentences were replaced, which row was inserted, which lines were removed).
- The new title.
- Link to PR #6946 view for reviewer reference.
- Test-count delta captured from Plans 01-03 SUMMARYs (useful for Phase 17 retrospective even though the counts were dropped from the body).
</output>
