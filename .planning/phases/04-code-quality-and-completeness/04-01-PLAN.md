---
phase: 04-code-quality-and-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/main/java/io/questdb/std/DoubleSort.java
  - core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java
  - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java
  - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileContDoubleWindowFunctionFactory.java
  - core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileDiscDoubleWindowFunctionFactory.java
  - core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileContDoubleWindowFunctionFactory.java
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "quickSort/partition/swap exist in exactly one place: DoubleSort.java"
    - "All 4 window factories delegate sorting to DoubleSort.sort()"
    - "GroupByDoubleList.sort() delegates to DoubleSort.sort()"
    - "All existing percentile tests pass unchanged after refactoring"
  artifacts:
    - path: "core/src/main/java/io/questdb/std/DoubleSort.java"
      provides: "Shared off-heap double quicksort utility"
      contains: "public static void sort(long ptr, long left, long right)"
    - path: "core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java"
      provides: "Delegates sort to DoubleSort instead of 380-line dual-pivot sort"
      contains: "DoubleSort.sort"
  key_links:
    - from: "PercentileDiscDoubleWindowFunctionFactory.java"
      to: "DoubleSort.sort()"
      via: "static method call replacing private quickSort"
      pattern: "DoubleSort\\.sort"
    - from: "GroupByDoubleList.java"
      to: "DoubleSort.sort()"
      via: "addressOf(0) passed as ptr"
      pattern: "DoubleSort\\.sort\\(addressOf"
---

<objective>
Extract duplicated quickSort/partition/swap from 8 window factory locations and GroupByDoubleList into a single shared DoubleSort utility class, then update all callers.

Purpose: Eliminate 9 copies of sort logic (8 window + 1 GroupByDoubleList) into one DoubleSort.java, reducing maintenance burden and preventing sort-related bugs from diverging across copies.
Output: DoubleSort.java in io.questdb.std, all window factories and GroupByDoubleList delegating to it, 24+ dead private methods removed from window factories, ~380 lines removed from GroupByDoubleList.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-correctness/01-02-SUMMARY.md
@.planning/phases/03-performance/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From core/src/main/java/io/questdb/std/LongSort.java (naming convention reference):
```java
package io.questdb.std;
public class LongSort {
    public static final int INSERTION_SORT_THRESHOLD = 47;
    // ...
}
```

From core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java:
```java
public long addressOf(int index) {
    return ptr() + HEADER_SIZE + 8L * index;
}
public void sort(int lo, int hi) {
    // lines 192-213: bounds check then delegates to private sort()
    // lines 214-578: ~380 lines of dual-pivot quicksort
}
```

Window factory inner class sort pattern (identical in all 8 locations):
```java
// Partitioned variant (e.g. PercentileDiscOverPartitionFunction):
private void quickSort(long listPtr, long left, long right) { ... }
private long partition(long listPtr, long left, long right) { ... }
private void swap(long listPtr, long i, long j) { ... }
// Called as: quickSort(listPtr + DATA_OFFSET, 0, size - 1);

// WholeResultSet variant (e.g. PercentileDiscOverWholeResultSetFunction):
private void quickSort(long left, long right) { ... }
private long partition(long left, long right) { ... }
private void swap(long i, long j) { ... }
// Called as: quickSort(0, size - 1);
```

Phase 1 Plan 02 replaced the Lomuto partition with Dutch National Flag three-way partition
and added insertion sort fallback at threshold 47. The current quickSort in the window
factories already uses this improved algorithm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DoubleSort utility class in io.questdb.std</name>
  <files>core/src/main/java/io/questdb/std/DoubleSort.java</files>
  <read_first>
    - core/src/main/java/io/questdb/std/LongSort.java (naming convention, package location, license header)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java lines 302-370 (current quickSort/partition/swap to extract -- the partitioned variant uses Dutch National Flag three-way partition from Phase 1)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java lines 458-520 (WholeResultSet variant -- shares same algorithm but accesses memory with base address 0)
  </read_first>
  <action>
Create new file `core/src/main/java/io/questdb/std/DoubleSort.java` per D-01 and D-02.

Package: `io.questdb.std`
Class: `public class DoubleSort`

Copy the QuestDB Apache 2.0 license header from LongSort.java.

Extract the quickSort algorithm currently in the window factories (Dutch National Flag three-way partition with insertion sort fallback at threshold 47, as implemented in Phase 1 Plan 02). The API takes raw native pointers:

```java
public static void sort(long ptr, long left, long right)
```

Where `ptr` is the native address of element[0], and elements are accessed via `Unsafe.getUnsafe().getDouble(ptr + index * 8L)`.

Internal private methods (alphabetically per QuestDB convention):
- `private static double getDouble(long ptr, long index)` -- reads double at ptr + index * 8L
- `private static void insertionSort(long ptr, long left, long right)` -- fallback for small subarrays
- `private static long medianOfThree(long ptr, long a, long b, long c)` -- returns index of median element
- `private static void putDouble(long ptr, long index, double value)` -- writes double at ptr + index * 8L
- `private static void swap(long ptr, long i, long j)` -- swaps elements at positions i and j

The sort method uses the Dutch National Flag (three-way) partitioning scheme that handles all-equal data in O(N) -- this was the key fix from Phase 1 Plan 02 (COR-02). Elements equal to pivot are skipped, preventing O(N^2) degradation.

Use `INSERTION_SORT_THRESHOLD = 47` as a private static final constant (matching existing window factory implementations and LongSort convention).

Do NOT extract quickSelect -- it stays in GroupByDoubleList/GroupByLongList per D-02.

Member ordering per CLAUDE.md: public static methods first (sort), then private static constants (INSERTION_SORT_THRESHOLD), then private static methods (alphabetically: getDouble, insertionSort, medianOfThree, putDouble, swap). Actually -- constants before methods. Follow the exact ordering convention from LongSort.java.
  </action>
  <verify>
    <automated>cd /Users/sminaev/qdbwt/abstract-crafting-sunrise && javac -cp core/target/classes core/src/main/java/io/questdb/std/DoubleSort.java 2>&1 | head -20; grep -c "public static void sort" core/src/main/java/io/questdb/std/DoubleSort.java; grep -c "private static" core/src/main/java/io/questdb/std/DoubleSort.java</automated>
  </verify>
  <acceptance_criteria>
    - core/src/main/java/io/questdb/std/DoubleSort.java exists
    - File contains `package io.questdb.std;`
    - File contains `public class DoubleSort`
    - File contains `public static void sort(long ptr, long left, long right)`
    - File contains `private static double getDouble(long ptr, long index)`
    - File contains `private static void swap(long ptr, long i, long j)`
    - File contains `INSERTION_SORT_THRESHOLD`
    - File does NOT contain `quickSelect`
    - File contains `Unsafe.getUnsafe().getDouble(ptr + index * 8L)` or equivalent
  </acceptance_criteria>
  <done>DoubleSort.java exists with sort(), swap(), getDouble(), putDouble() and uses Dutch National Flag three-way partition with insertion sort fallback. No quickSelect present.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 9 sort implementations with DoubleSort.sort() calls</name>
  <files>
    core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java
    core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java
    core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileContDoubleWindowFunctionFactory.java
    core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileDiscDoubleWindowFunctionFactory.java
    core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileContDoubleWindowFunctionFactory.java
  </files>
  <read_first>
    - core/src/main/java/io/questdb/std/DoubleSort.java (just created in Task 1)
    - core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java lines 84-86 (addressOf method) and lines 192-578 (sort methods to replace)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java (full file -- find all quickSort/partition/swap methods and call sites)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileContDoubleWindowFunctionFactory.java (full file)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileDiscDoubleWindowFunctionFactory.java (full file)
    - core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileContDoubleWindowFunctionFactory.java (full file)
  </read_first>
  <action>
**GroupByDoubleList.java (D-03):**

Replace the `sort(int lo, int hi)` method body. Keep the bounds check. Replace the delegation:

Before:
```java
public void sort(int lo, int hi) {
    if (lo < 0 || hi >= size()) {
        throw new ArrayIndexOutOfBoundsException(...);
    }
    if (lo < hi) {
        sort(lo, hi, true);  // calls 380-line dual-pivot quicksort
    }
}
```

After:
```java
public void sort(int lo, int hi) {
    if (lo < 0 || hi >= size()) {
        throw new ArrayIndexOutOfBoundsException(...);
    }
    if (lo < hi) {
        DoubleSort.sort(addressOf(0), lo, hi);
    }
}
```

Delete the entire `private void sort(int left, int right, boolean leftmost)` method and all its helper methods (`merge`, `rangeCheck`, etc.) -- approximately lines 214-578. This removes ~380 lines of dual-pivot quicksort code.

Add import: `import io.questdb.std.DoubleSort;`

**4 Window factories (D-04) -- each has 2 inner classes (Partitioned + WholeResultSet):**

For each PARTITIONED inner class, replace the quickSort call site:
Before: `quickSort(listPtr + DATA_OFFSET, 0, size - 1);`
After: `DoubleSort.sort(listMemory.getPageAddress(0) + listPtr + DATA_OFFSET, 0, size - 1);`

For each WHOLE RESULT SET inner class, replace the quickSort call site:
Before: `quickSort(0, size - 1);`
After: `DoubleSort.sort(listMemory.getPageAddress(0), 0, size - 1);`

After updating all 8 call sites, DELETE all 24 private helper methods (8 quickSort + 8 partition + 8 swap) from the 4 factory files' inner classes. These are now dead code.

Also delete any `INSERTION_SORT_THRESHOLD` constants from the factory files since the threshold is now in DoubleSort.

Add import `import io.questdb.std.DoubleSort;` to all 4 factory files.

Verify: Run the full percentile test suites to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/sminaev/qdbwt/abstract-crafting-sunrise && mvn -pl core -Dtest=PercentileWindowFunctionTest,PercentileDiscDoubleGroupByFunctionFactoryTest,PercentileDiscLongGroupByFunctionFactoryTest,PercentileContDoubleGroupByFunctionFactoryTest,MultiPercentileDiscDoubleGroupByFunctionFactoryTest,MultiPercentileContDoubleGroupByFunctionFactoryTest test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "private.*quickSort\|private.*partition\|private.*swap" core/src/main/java/io/questdb/griffin/engine/functions/window/Percentile*WindowFunctionFactory.java core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentile*WindowFunctionFactory.java` returns zero matches
    - `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileDiscDoubleWindowFunctionFactory.java` returns 2 (one per inner class)
    - `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/functions/window/PercentileContDoubleWindowFunctionFactory.java` returns 2
    - `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileDiscDoubleWindowFunctionFactory.java` returns 2
    - `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentileContDoubleWindowFunctionFactory.java` returns 2
    - `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java` returns 1
    - `grep -rn "private void sort(int left, int right, boolean leftmost)" core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java` returns zero matches (dual-pivot sort deleted)
    - All percentile tests pass (PercentileWindowFunctionTest, all group-by factory tests)
  </acceptance_criteria>
  <done>All 9 sort implementations replaced with DoubleSort.sort() calls. 24 dead private methods removed from window factories. ~380 lines removed from GroupByDoubleList. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native memory access | DoubleSort reads/writes via Unsafe at caller-provided ptr+offset -- incorrect ptr causes segfault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | DoubleSort.sort() | accept | Pure refactoring -- same memory access pattern as existing code. No new trust boundary introduced. Callers already validated bounds before Phase 4. |
| T-04-02 | D (Denial of Service) | DoubleSort.sort() | mitigate | Three-way partition prevents O(N^2) on all-equal data (carried forward from Phase 1 COR-02 fix). Insertion sort fallback for small arrays. |
</threat_model>

<verification>
1. `grep -rn "quickSort\|private.*partition\|private.*swap" core/src/main/java/io/questdb/griffin/engine/functions/window/Percentile* core/src/main/java/io/questdb/griffin/engine/functions/window/MultiPercentile*` returns only call-site comments (no method definitions)
2. `grep -c "DoubleSort.sort" core/src/main/java/io/questdb/griffin/engine/functions/window/*.java core/src/main/java/io/questdb/griffin/engine/groupby/GroupByDoubleList.java` shows exactly 9 call sites (8 window + 1 GroupByDoubleList)
3. All percentile test suites pass with zero failures
</verification>

<success_criteria>
- DoubleSort.java exists in io.questdb.std with public static sort(long, long, long)
- Zero quickSort/partition/swap private methods remain in any window factory
- GroupByDoubleList.sort() delegates to DoubleSort.sort(addressOf(0), lo, hi)
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-and-completeness/04-01-SUMMARY.md`
</output>
